# Paw Spot Global — Design Guidelines (v1.6)

> 이 문서는 Paw Spot Global의 UI를 구현·수정할 때 따르는 기준이다.  
> 에어비앤비의 탐색 구조와 절제된 시각 언어, 당근의 정보 밀도와 지역 중심 UX를 참고하되 두 브랜드의 색상·로고·고유 컴포넌트를 복제하지 않는다.

## 변경 이력

최신 판이 위에 온다.

**v1.6 (2026-09-08)** — §13 C 묶음을 구현하며 확정한 표시 규칙을 반영했다. 문서만 고친 판이 아니라 **코드·테스트·브라우저 확인이 함께 끝난 판**이다.

- §9: 복합 상태의 한국어를 `실내 동반 불가 · 야외 동반 미확인`으로 바꿨다. 조건의 미확인은 `미확인`으로 통일하고, 신선도 `재확인 필요`·상태 제목 `방문 전 확인 필요`·행동 안내 `매장에 문의`와 구분한다고 적었다.
- §9: **구역 기록 규칙**을 신설했다 — `access: UNKNOWN`은 근거로 쓰지 않고, 출입은 `spaceExceptions`에서만 읽으며, 구역·적용 대상을 넓히지 않고, 실내·야외 모두 불가여도 장소 전체 `NOT_ALLOWED`로 바꾸지 않는다.
- §9: **명백한 정보 불일치** 처리를 넣었다 — 판정 `unknown`, 조건 요약 `동반 조건 정보 불일치` / `Conflicting dog access information`. 전체 반려견 대상 기록만 충돌이 될 수 있고 크기·세부 구역 예외는 정상이다.
- §6 Place Card: 조건 영역 규칙을 고쳤다. **핵심 조건 3개는 항목 수이지 줄 수가 아니며**, 공통 최소 높이로 기본 정렬을 맞추되 긴 문구는 필요한 만큼 확장한다. 동일 높이보다 **가독성과 완전성을 우선한다.**
- §13: C 묶음을 완료로 옮기고, 남은 문구(C-8·C-9)와 **저장 단계 모순 검증(G-1)**을 새로 적었다.

**v1.5 (2026-09-08)** — v1.4의 오류를 고치고 빠진 기준을 채웠다. v1.4가 확정한 결정 자체는 그대로다.

- §9: **`No carrier/stroller`를 폐기했다.** `필수가 아니다`라는 뜻이 사라진다. `불필요` 상태의 축약형을 문맥에 따라 `Carrier/stroller not required`(값만 보이는 곳)와 `Not required` / `필수 아님`(라벨이 붙은 곳)으로 나누고, 카드에는 조건 이름이 없어 첫 번째를 쓴다는 점을 적었다. `필수 아님`이 반입 금지나 사용 허용을 뜻하지 않는다는 규칙도 넣었다.
- §9: **`실내 불가 · 야외 미확인`의 표시 기준**을 신설했다. 두 사실을 함께 전달하는 문구를 확정하고(`No indoor access · Outdoor access unconfirmed` / `실내 동반 불가 · 야외 이용 여부 확인 필요`), 쓰지 않을 표시 셋과 카드·미리보기·상세의 최소 조건을 적었다. 저장 구조의 식별자는 실제 스키마와 대조했고, **저장 가능과 표시 가능을 분리해** 요약 컬럼만 읽는 현재 구현을 후속 대상으로 남겼다.
- §4 `그림자 기준`: 후속 목록의 집계 오류를 고쳤다. `shadow-sm` 17곳은 **제거 15 + 교체 1 + 보류 1**이며, 필터 Drawer의 `shadow-2xl`은 별개 항목이다. 보류 2곳은 현재 값을 유지하는 항목이지 결정 대기 항목이 아니다.
- §6: 카드 예시·밀도 검증 예시를 위 어휘에 맞추고, **축약으로 짧아지지 않는 조건**이 있다는 것을 밀도 규칙에 적었다.
- **§13 신설** — 여러 판에 흩어져 있던 문서 대비 구현 후속 작업을 한 목록으로 모으고 권장 순서를 붙였다. 미사용 코드 정리는 기능 작업과 분리했다.

**v1.4 (2026-09-08)** — v1.3.1이 남긴 **확정 대기 두 건**을 닫았다. 새 규칙을 만들기보다 이미 쓰이고 있던 의미를 문서에 고정했다.

- §9: 실내 조건 `NOT_ALLOWED`의 뜻을 **장소 전체 반려견 동반 불가**로 확정했다. 어휘표의 `확정 대기` 행과 `표시 문구 확정 대기` 단서를 지웠다. 필드명 `indoor` / `IndoorPolicy`가 실제 의미 범위보다 좁다는 점, 그리고 `실내 불가만 확인되고 야외는 불명`인 경우를 현재 구조로 담을 때의 제약을 함께 적었다.
- §4 Border and Elevation: **일반 콘텐츠 카드에 그림자를 쓰지 않는다**로 확정하고, 떠 있는 레이어 · 지도 위 요소 · 그 밖의 사용처를 역할별로 나눴다. 각 유틸리티가 어떤 CSS 값으로 연결되는지도 적었다. 이전 판의 `선택 카드에 약한 그림자 허용`은 없앴다 — 선택 상태는 §6 Place Card대로 테두리와 배경으로만 표현한다.
- §4 Radius: **Skeleton을 12px 고정에서 뺐다.** Skeleton은 대체하는 요소의 radius를 따른다.
- §9: `이동장·유모차 필수`를 **`이동장 또는 유모차 필수`**로 고치고, 둘 다 필요한 경우 · 한 종류만 허용하는 경우와 구분하는 규칙을 넣었다.
- §6: 카드 예시와 밀도 검증 예시의 `No carrier needed`, Condition Row의 라벨 `Carrier`가 **유모차를 빠뜨리고 있어** §9 어휘표에 맞추었다. *(이때 쓴 `No carrier/stroller`는 v1.5에서 폐기했다 — 위를 본다.)*

**v1.3.1 (2026-09-08)** — v1.3 직후의 후속 정리. 규칙을 새로 만들기보다 뜻이 불분명하던 곳을 좁혔다.

- §4 Radius: **콘텐츠 카드를 16px로 통일**하고 12px을 카드가 아닌 패널·표면 용도로 한정했다. 공통 스케일 6단계를 명시하고, 히어로 일러스트·장식 컨테이너 전용 **24px 한정 예외**를 스케일과 구분해 적었다.
- §9: **실내 동반 불가와 전체 동반 불가**를 분리하고 표시 원칙 4개를 넣었다. 현재 데이터의 실내 `NOT_ALLOWED`가 어느 쪽인지 확정되지 않아 해당 행은 **확정 대기**로 두었다.
- §9: 이동장·유모차 문구에서 **유모차 의미가 빠지지 않도록** 하고, 미확인은 **필요 여부**가 확인되지 않았음을 밝히도록 고쳤다. 최대 허용 크기는 **상한임이 드러나는 형식**으로 바꾸고 `Large allowed` 같은 불완전한 문구를 뺐다.
- §9: 축약형은 **같은 화면의 라벨·문맥과 함께 봐도 뜻이 보존될 때만** 허용하도록 조건을 달았다.
- §11: 상태 변경 안내를 **확정된 요구사항 3개**와 **현재 구현의 미검증**으로 분리했다. 미검증이라는 이유로 요구사항이 흐려지지 않게 했다.

**v1.3 (2026-09-08)** — 문서 전체 감사 후 정비. 규칙을 바꾼 곳과 빠져 있던 것을 채운 곳이 섞여 있다.

- §4 `--color-danger`를 `#d51f1f`로 조정하고 `--color-border-control`(`#828b98`)을 신설했다. 토큰별 **허용 배경과 실측 대비**를 표로 넣었다.
- §4에 **Breakpoints**와 **Token aliases** 하위 절을 신설했다. 둘 다 이미 쓰이고 있었으나 문서에 없었다.
- §4 Radius 스케일에 **10px**을 포함하고 값별 용도를 나눴다.
- §4 Spacing이 여백 척도이고 컴포넌트 규격(높이 등)과 별개임을 명시했다.
- §5 Global Header 높이를 **64px 단일**로 정리하고, Header와 `main` 랜드마크의 **소유 위치**를 규정했다.
- §5 Places Mobile의 Bottom Sheet를 **3단계**로 고쳤다(기존 "두 단계"는 현재 화면과 달랐다). 목록·지도 토글의 라벨 규칙을 넣었다.
- §6 컨트롤 크기를 **보이는 크기와 조작 영역**으로 분리하고 44×44 CSS px 규칙과 관리자 예외를 정리했다.
- §6 Place Preview footer를 현재 배치(1줄 3버튼)로 고치고 잘림 금지 조건을 넣었다.
- §7 `Load more`를 현재 동작과 페이징 도입 시 규칙으로 나눴다.
- §8 reduced motion에서 제거할 것과 남길 것을 구분했다.
- §9에 **조건 상태 어휘표**(한국어·영어 정식 문구와 허용 축약형)를 신설했다.
- §10 거리·확인일 표기의 언어별 규칙과 경계 예시를 정리했다.
- §11을 **WCAG 표준 요구**와 **프로젝트 자체 규칙**으로 나누고, D-13a에 따라 **모달 Dialog와 비모달 탐색 시트의 focus 규칙을 분리**했다.
- `Included Components`가 파일 목록이 아니라 **디자인이 다루는 UI 요소 목록**임을 명시했다.

**v1.2.2 (2026-09-07)**: §7 States의 위치·빈 상태 행을 D-11에 맞췄다. `Seoul City Hall` fallback 표기를 대전으로 바꾸고, 서비스 범위 밖 상태와 빈 상태 2종 분리를 표에 넣었다.

**v1.2.1 (2026-09-06)**: D-02의 경계를 **`마지막 확인일로부터 90일 이상 경과`(>= 90일)**로 확정했다. §6은 "90일 이상", §7 상태표는 "90일 초과"로 적혀 하루 어긋나 있던 것을 §6 기준으로 통일했다.

**v1.2** (기획서 v3 결정 반영): 정보 신선도 임계값을 8주에서 **90일 단일 기준**으로 통일(D-02), Korean Inquiry Box를 **MVP 제외 / P1 재도입 예정**으로 표기(D-01).

**v1.1**: 다크 모드 정책, z-index 체계, 아이콘 규칙, 텍스트 색상 용도 구분, Place Card 밀도 검증 기준, 한국어 UI의 문의 박스 처리, 거리 표기 규칙 추가.

## 1. Product Direction

- 대상: 한국에 거주하거나 여행 중인 영어 사용 반려견 보호자
- 핵심 가치: **지도와 필터로 내 주변에서 반려견 동반 조건이 맞는 장소를 찾고, 방문 전에 조건과 정보의 신뢰도를 판단하게 한다.**
- 기본 언어: 영어
- 보조 언어: 한국어
- 핵심 탐색 정보:
  1. 거리와 위치
  2. 실내 동반 가능 여부
  3. 이동장·유모차 조건
  4. 허용 반려견 크기
  5. 마지막 확인일과 확인 방법

## 2. Reference Rules

### From Airbnb

- 흰색 중심의 차분한 화면
- 장소 사진, 카드, 지도 사이의 자연스러운 연결
- 둥근 필터와 간결한 탐색 컨트롤
- 테두리와 여백 중심의 계층
- 구체적이고 행동 중심적인 문구

사용하지 않는다:

- Airbnb Pink `#ff385c`
- Airbnb Cereal
- Guest, Host, Booking, Reserve 중심 구조
- 사진이 조건 정보보다 우선하는 `photography-first` 구성
- 과도하게 큰 검색 바와 모든 버튼의 pill 처리

### From Karrot

- 4px 기반 간격 체계
- 한 화면에서 여러 장소를 비교할 수 있는 정보 밀도
- 지역명과 거리의 상시 노출
- 과한 장식과 그림자가 없는 평면적 UI
- 따뜻하지만 과장하지 않는 문체

사용하지 않는다:

- Karrot Orange `#ff6f0f` 또는 `#ff6600`
- 중고 거래 카드와 채팅 중심 구조
- 당근 캐릭터·브랜드 서사
- 모든 화면에 강한 브랜드 색을 반복하는 방식

## 3. Core Principles

### 3.1 Visit eligibility leads

사진보다 먼저 사용자가 자신의 반려견과 방문할 수 있는지 판단하게 한다. `Pet-friendly`라는 포괄적 배지 대신 구체적인 조건을 표시한다.

### 3.2 Map, list, and preview behave as one

장소 카드, 미리보기 패널, 지도 마커는 동일한 선택 상태를 공유한다. 어느 영역에서 장소를 선택해도 나머지 영역이 즉시 동기화되어야 한다.

### 3.3 Trust is explicit

검증 여부만 단독 배지로 표시하지 않는다. 확인일과 확인 방법을 함께 보여주고, 모르는 정보는 미확인 상태로 명확히 표시한다. 상태별 정식 문구는 §9 `조건 상태 어휘`가 정한다.

### 3.4 Proximity is always visible

장소 카드와 상세 요약에는 거리와 지역명을 숨기지 않는다. 위치가 확인되지 않았다면 임의의 거리를 표시하지 않는다.

### 3.5 One primary action per context

한 영역에서 파란색 채움 버튼은 하나만 사용한다. 나머지 행동은 outline, ghost 또는 text action으로 낮춘다.

### 3.6 Photos support the decision

사진은 장소 분위기와 식별을 돕는 정보다. 사진 때문에 동반 조건, 거리, 검증 정보가 첫 화면에서 밀려나면 안 된다.

## 4. Design Tokens

### Colors

```css
:root {
  --color-primary: #2563eb;
  --color-primary-hover: #1d4ed8;
  --color-primary-soft: #eff6ff;
  --color-on-primary: #ffffff;

  --color-canvas: #ffffff;
  --color-background: #f7f8fa;
  --color-surface: #ffffff;
  --color-surface-subtle: #f2f4f7;

  --color-text: #171719;
  --color-text-secondary: #5f6368;
  --color-text-muted: #6b7280;
  --color-border: #e5e7eb;
  --color-border-strong: #cfd4dc;
  --color-border-control: #828b98;

  --color-success: #15803d;
  --color-success-soft: #f0fdf4;
  --color-warning: #b45309;
  --color-warning-soft: #fffbeb;
  --color-danger: #d51f1f;
  --color-danger-soft: #fef2f2;
  --color-unknown: #667085;
  --color-unknown-soft: #f2f4f7;

  --color-focus: #2563eb;
  --color-overlay: rgb(17 24 39 / 48%);
}
```

- Primary Blue: 주요 CTA, 선택 필터, 선택 마커, focus
- Green: 조건 충족 또는 최근 확인
- Amber: 조건부 허용, 오래된 정보, 주의 필요
- Red: 동반 불가, 입력 오류, 위험한 작업
- Gray: 미확인, 비활성, 보조 정보
- 상태는 색상만으로 표현하지 않고 아이콘과 텍스트를 함께 사용한다.

#### Text color usage

- `--color-text`: 장소명, 조건 값, 본문 등 판단에 필요한 모든 1차 정보
- `--color-text-secondary`: 카테고리, 지역명, 확인 방법 등 1차 정보를 보조하는 라인
- `--color-text-muted`: placeholder, caption, 비활성 텍스트, 부가 디스클레이머 — 단 아래 허용 배경에서만
- 같은 텍스트 블록 안에서 secondary와 muted를 혼용하지 않는다. 구분이 애매하면 secondary를 사용한다.

**muted의 허용 배경** — `--color-text-muted`는 밝은 회색이라 배경을 가린다. 아래 배경에서만 쓴다.

| 배경 | 대비 | muted 사용 |
|---|---|---|
| `--color-surface` / `--color-canvas` `#ffffff` | 4.8345 | 가능 |
| `--color-background` `#f7f8fa` | 4.5495 | 가능 |
| `--color-surface-subtle` `#f2f4f7` | 4.3876 | **불가 → `--color-text-secondary`** |
| `--color-primary-soft` `#eff6ff` | 4.4423 | **불가 → `--color-text-secondary`** |

- `surface-subtle` 위에 놓이는 **작은 안내문(디스클레이머·caption)과 조건 요약 텍스트**는 `--color-text-secondary`를 쓴다. 같은 자리에서 secondary는 5.4905로 여유가 있다.
- 카드처럼 **상태에 따라 배경이 바뀌는 표면**은 가장 어두운 배경을 기준으로 색을 고른다. 예를 들어 기본 배경이 흰색이어도 hover에서 `surface-subtle`, 선택에서 `primary-soft`가 되는 표면의 본문 텍스트에는 muted를 쓰지 않는다.
- 이 규칙은 색을 옅게 보이려고 투명도를 겹치는 방식에도 똑같이 적용한다. 최종적으로 눈에 보이는 색으로 판단한다.

#### Border color usage

테두리 토큰은 **역할이 다르다.** 굵기가 아니라 그 선이 무엇을 하는지로 고른다.

| 토큰 | 값 | 용도 | 흰 배경 대비 |
|---|---|---|---|
| `--color-border` | `#e5e7eb` | 카드 외곽선, 구분선, 섹션 경계 등 **구조·장식** 선 | 1.2381 |
| `--color-border-strong` | `#cfd4dc` | 위와 같은 구조 선 중 **한 단계 강조**가 필요한 곳, hover 시 카드 테두리 | 1.4889 |
| `--color-border-control` | `#828b98` | **경계가 있어야 컨트롤임을 알 수 있는 조작 요소**의 테두리 | 3.4451 |

- `--color-border-control`을 쓰는 곳: **텍스트 입력·검색 필드·select 계열**처럼 테두리 말고는 입력 영역을 알 방법이 없는 컨트롤. 이 경우 테두리가 곧 컨트롤의 식별 정보다.
- **아웃라인 버튼과 비선택 필터 칩**에도 같은 토큰을 쓴다. 이유는 접근성 강제 때문이 아니라 **같은 컨트롤 계열이 한 화면에서 다른 굵기의 회색으로 보이지 않게** 하기 위해서다. 이 컨트롤들은 보이는 텍스트 라벨이 있어 테두리가 식별의 유일한 수단은 아니다.
- 카드 외곽선·구분선·표 줄에는 `--color-border-control`을 쓰지 않는다. 구조 선까지 진해지면 §2가 정한 절제된 인상이 무너진다.
- 선택 상태를 테두리 색 하나로만 표현하지 않는다. 선택 칩은 테두리·배경·텍스트가 함께 바뀐다(§6 Filter Bar).

#### Contrast reference

측정값은 WCAG 2.x 상대휘도 공식으로 계산했고, **반올림 전 값으로 판정**한다. 아래는 실제로 화면에 나타나는 조합만 적었다.

| 전경 | 배경 | 대비 | 기준 |
|---|---|---|---|
| `text` `#171719` | `surface` / `surface-subtle` / `primary-soft` | 17.9024 / 16.2475 / 16.4501 | 4.5 |
| `text-secondary` `#5f6368` | `surface` / `surface-subtle` / `primary-soft` | 6.0498 / 5.4905 / 5.5590 | 4.5 |
| `text-muted` `#6b7280` | `surface` / `background` | 4.8345 / 4.5495 | 4.5 |
| `danger` `#d51f1f` | `surface` / `surface-subtle` / `primary-soft` / `danger-soft` | 5.1916 / 4.7117 / 4.7704 / 4.7459 | 4.5 |
| `warning` `#b45309` | `warning-soft` | 4.8424 | 4.5 |
| `success` `#15803d` | `success-soft` | 4.7914 | 4.5 |
| `unknown` `#667085` | `unknown-soft` | 4.5149 | 4.5 |
| `primary` `#2563eb` | `surface` | 5.1686 | 4.5 |
| `on-primary` `#ffffff` | `primary` / `primary-hover` | 5.1686 / 6.7016 | 4.5 |
| `on-primary` `#ffffff` | `danger` `#d51f1f` | 5.1916 | 4.5 |
| `border-control` `#828b98` | `surface` / `surface-subtle` / `primary-soft` / `background` | 3.4451 / 3.1266 / 3.1656 / 3.2420 | 3.0 |
| `focus` `#2563eb` | `surface` / `surface-subtle` | 5.1686 / 4.6908 | 3.0 |

- 토큰 값을 바꿀 때는 **그 토큰이 놓이는 모든 배경**을 다시 계산한다. 흰 배경 하나만 보고 판단하지 않는다.
- 새 조합을 만들 때도 위 표에 행을 추가한다. 기준을 못 넘으면 색을 조정하고, **예외로 적어 통과시키지 않는다.**

#### Dark mode

- v1은 라이트 모드만 지원한다. 다크 모드 토큰과 스타일을 임의로 추가하지 않는다.
- `prefers-color-scheme: dark`에 대응하는 자동 반전을 적용하지 않는다.
- 다크 모드 도입 시 이 문서의 토큰 확장으로 진행하며, 컴포넌트별 하드코딩 색상을 만들지 않는다.

### Z-index

```css
:root {
  --z-map-control: 10;
  --z-dropdown: 50;
  --z-header: 100;
  --z-drawer: 200;
  --z-bottom-sheet: 300;
  --z-dialog: 400;
  --z-snackbar: 500;
  --z-tooltip: 600;
}
```

- 위 토큰 외의 임의 z-index 값을 사용하지 않는다.
- Dropdown은 페이지 콘텐츠 위, Header 아래에 위치한다.
- overlay(`--color-overlay`)는 해당 레이어 바로 아래에 위치한다.
- Snackbar는 Dialog 위에도 표시될 수 있다.
- 지도 라이브러리 내부 z-index는 `--z-map-control` 이하로 제한한다.

### Icons


- 아이콘 셋: lucide 단일 셋만 사용한다. 다른 셋과 혼용하지 않는다.
- 크기: 16px(조건 행, 배지, 인라인), 20px(버튼, 필터, 목록 액션), 24px(헤더, 빈 상태)
- stroke: 기본 1.5, 16px에서는 2를 허용한다.
- 색상은 함께 있는 텍스트 색상을 따른다. 상태 아이콘은 상태 색상 토큰을 사용한다.
- 의미 전달용 아이콘에는 텍스트 라벨을 병기한다. 장식용 아이콘은 `aria-hidden`으로 처리한다.
- 같은 의미에는 항상 같은 아이콘을 사용한다. (예: 실내 동반, 이동장, 크기, 확인 상태)

### Typography

현재 프로젝트의 `next/font` 기반 sans-serif를 유지한다. Airbnb Cereal이나 별도 브랜드 폰트를 추가하지 않는다.

```text
Display       40/48  700  홈 히어로 전용
Page title    28/36  700
Section title 20/28  700
Card title    16/24  600
Body          14/21  400
Body strong   14/21  600
Caption       12/18  400
Button        14/20  600
```

- 영어와 한국어 모두 가독성을 우선한다.
- 본문은 최소 14px, 주요 조건은 최소 14px로 표시한다.
- 장소명은 최대 2줄, 버튼과 상태 문구는 가능한 한 자르지 않는다.

### Spacing

**여백(margin·padding·gap)** 척도다. 4px grid만 사용한다.

```text
4, 8, 12, 16, 20, 24, 32, 40, 48, 64
```

- 카드 내부: 12–16px
- 패널 내부: 16–24px
- 섹션 사이: 24–32px
- 모바일 화면 좌우: 16px

- 이 척도는 **컴포넌트 규격이 아니다.** 버튼·입력·칩의 높이와 조작 영역은 §6 Buttons and Inputs가 정한다. 그쪽에서 정한 44px처럼 이 목록에 없는 값이 나올 수 있다.
- 세로 여백과 가로 여백에 같은 단계를 강제하지 않는다. 밀도가 목적인 목록에서는 세로를 한 단계 줄여도 된다.

### Breakpoints

Tailwind 기본 breakpoint를 그대로 쓴다. 별도 토큰을 만들지 않는다.

```text
sm    640px
md    768px
lg   1024px
xl   1280px
2xl  1536px
```

- 탐색 화면의 전환 기준은 §5 Layout이 정한다. `lg` 미만은 지도 + Bottom Sheet, `lg`–`xl` 미만은 목록 + 지도 + overlay 미리보기, `xl` 이상은 3분할이다.
- 화면 폭이 아니라 **입력 방식**으로 갈리는 규칙(터치 조작 영역 등)은 breakpoint로 분기하지 않는다. §6이 정한 조작 영역은 폭과 무관하게 적용한다.

### Token aliases

이 문서의 `--color-*`가 **원본**이다. 그 위에 shadcn/ui가 요구하는 별칭 계층이 있고, Tailwind 유틸리티는 둘 중 하나를 가리킨다.

```text
--color-primary  →  --primary        →  bg-primary / text-primary
--color-danger   →  --destructive    →  bg-destructive
--color-border   →  --border         →  border-border
(별칭 없음)       →  --color-border-strong  →  border-border-strong
```

- 별칭은 **원본을 참조만 한다.** 별칭에 색 값을 직접 적지 않는다. 색을 바꿔야 하면 `--color-*`를 바꾼다.
- 새 토큰을 추가할 때 shadcn 컴포넌트가 요구하지 않으면 별칭을 만들지 않는다. `--color-border-control`은 별칭 없이 `border.control` 유틸리티로만 노출한다.
- 같은 역할에 원본과 별칭이 섞여 매핑되면 어느 쪽을 바꿔야 할지 알 수 없어진다. 한 역할에는 한 경로만 둔다.

### Radius

**공통 스케일은 6단계다.** 8px · 10px · 12px · 16px · 999px(pill) · 50%(circle).

```text
 8px    조밀한 컨트롤 — 메뉴 항목, 작은 아이콘 버튼, 소형 배지
10px    기본 컨트롤 — 버튼, 입력, 드롭다운 트리거, 드롭다운 표면
12px    패널·표면 컨테이너 — 미리보기 overlay 패널, 페이지 섹션 컨테이너
16px    콘텐츠 카드, 모달 Dialog, Bottom Sheet 상단
999px   pill — 필터 칩, 상태 배지, 카테고리 탭
 50%    circle — 아바타, 원형 아이콘 버튼
```

**콘텐츠 카드는 16px 하나로 통일한다.** 장소 카드, 방문 전 안내 카드, 상세 페이지 정보 카드, 반려견 카드, 홈 카테고리 카드가 모두 여기 해당한다. **같은 역할의 카드를 근거 없이 12px과 16px로 나누지 않는다.**

12px은 카드가 아닌 표면에 남긴다. 카드처럼 보인다는 이유로 12px을 고르지 않는다. 판단 기준은 모양이 아니라 역할이다 — 콘텐츠 하나를 담아 목록에서 비교되는 것은 카드(16px), 화면 구조를 나누는 그릇은 패널(12px)이다.

**용도 한정 예외**

```text
24px    히어로의 일러스트·장식 컨테이너 전용
```

- 24px은 홈 히어로의 일러스트 자리와 장식 컨테이너에만 쓴다. **공통 스케일이 아니다.**
- 일반 카드·입력·버튼·패널에 24px을 쓰지 않는다. 이 예외를 다른 화면으로 확대하지 않는다.
- 히어로의 일러스트가 실제 이미지로 교체되면 이 예외의 필요 여부를 다시 본다.

**공통 규칙**

- 10px은 `--radius`의 값이다. 8px은 여기서 파생된다(`--radius - 2px`). 두 값은 같은 계열이며 **조밀한 정도**로 갈린다.
- **`--radius`를 바꾸면 10px과 8px이 함께 움직인다.** 파생 관계는 `sm = --radius - 4px`, `md = --radius - 2px`, `lg = --radius`이고, 12 · 16 · 24 · 999px은 파생되지 않는 독립 값이다. 전역 radius를 조정할 때 이 범위를 먼저 확인한다.
- 위 스케일과 한정 예외에 없는 값(6px 등)을 새로 쓰지 않는다. 필요하면 이 표를 먼저 고친다.
- pill은 필터 칩, 상태 배지, 카테고리 탭에만 주로 사용한다.
- 일반 버튼과 입력 필드를 무조건 pill로 만들지 않는다.
- **Skeleton은 고유한 radius를 갖지 않는다.** 자리를 대신할 요소의 값을 그대로 따른다 — 카드 자리의 Skeleton은 16px, 패널 자리의 Skeleton은 그 패널의 값, 컨트롤 자리의 Skeleton은 그 컨트롤의 값이다. Skeleton 전체를 12px 같은 한 값으로 묶지 않는다. 묶으면 로딩이 끝나는 순간 모서리가 바뀌어 자리표시가 실제 내용과 다른 모양이었음이 드러난다. 이 규칙은 radius뿐 아니라 **크기와 배치**에도 같이 적용된다(§7 States의 `Initial loading`).

- 이 표는 **역할 기준**이다. 어떤 값이 코드에서 많이 쓰인다는 사실만으로 그 사용이 이 표에 맞는다고 보지 않는다. 역할과 다른 값이 쓰인 곳은 고쳐야 할 대상으로 남긴다.

### Border and Elevation

- 기본 계층은 `1px solid var(--color-border)`로 표현한다.
- 조작 요소의 테두리는 §4 `Border color usage`가 정한 토큰을 쓴다. 구조 선과 컨트롤 경계에 같은 토큰을 쓰지 않는다.
- 큰 blur, 색이 들어간 shadow, glassmorphism을 사용하지 않는다.

#### 그림자 기준

**그림자는 `떠 있는가`로만 정한다.** 카드처럼 보인다거나 강조하고 싶다는 이유로 붙이지 않는다. 페이지 평면 위에 놓인 것은 테두리로 구분하고, 평면에서 떠올라 그 아래 내용을 가리는 것만 그림자를 쓴다.

| 역할 | 그림자 | 해당하는 것 | 근거 |
|---|---|---|---|
| 일반 콘텐츠 카드 | **없음** | 장소 카드, 방문 전 안내 카드, 상세 정보 카드, 반려견 카드, 홈 카테고리 카드, 관리자 목록의 장소 카드 | 이번 결정 |
| 페이지 안에 놓인 패널 · 섹션 컨테이너 | **없음** | 로그인 섹션, 오류 · 빈 상태 섹션, 관리자 표 컨테이너, 관리자 통계 타일 | 위 원칙에서 파생. 이들은 카드가 아니므로 카드 규칙이 직접 적용되지는 않지만, 평면 위에 놓여 있어 같은 결론이 된다 |
| 떠 있는 레이어 — 드롭다운 · 시트 · 모달 · Drawer | `shadow-lg` | 정렬 드롭다운, 견종 목록, 필터 Drawer, Bottom Sheet, 모달 Dialog, 미리보기 overlay 패널 | 이번 결정 (기존 값 유지) |
| 지도 위에 독립적으로 떠 있는 요소 | `shadow-md` | `내 위치` 버튼, 지도 위 상태 알림 | 이번 결정 (기존 값 유지) |
| 히어로의 장식 · 일러스트 컨테이너 | **별도 사용처** | 홈 히어로의 일러스트 자리 | 위 네 행으로 설명되지 않는다 |
| 컨트롤 내부의 선택 상태 표시 | **별도 사용처** | 언어 전환 segmented control의 선택 thumb | 위 네 행으로 설명되지 않는다 |

- **`별도 사용처`는 미결정이라는 뜻이다.** 카드도 떠 있는 레이어도 아니어서 이번 결정이 값을 정하지 않았다. 그림자 클래스가 붙어 있다는 이유만으로 제거 대상에 넣지 않는다. 각각 다음 조건에서 다시 본다.
  - **히어로 장식**: 실제 이미지로 교체되면 그때 필요 여부를 판단한다(§4 Radius의 24px 한정 예외와 같은 범위다).
  - **segmented control의 선택 thumb**: 선택된 항목을 배경 위로 들어 올리는 것이 목적이다. 배경색과 대비만으로 선택 상태가 구분된다고 확인되면 그때 뺀다.
- **선택 상태를 그림자로 표현하지 않는다.** 카드의 selected는 primary 테두리와 옅은 primary 배경이다(§6 Place Card).
- 같은 화면에서 그림자를 두 단계 이상 겹쳐 쌓지 않는다.
- 떠 있던 요소가 breakpoint에 따라 정적 배치로 바뀌면 그림자도 함께 제거한다(`shadow-none`). 컬럼으로 자리 잡은 패널은 더 이상 떠 있지 않다.

**유틸리티와 CSS 값**

`tailwind.config.ts`에 `boxShadow` 확장이 없다. 아래는 프로젝트가 재정의하지 않고 그대로 쓰는 Tailwind 3.4 기본값이다.

```text
shadow-md    0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1)
shadow-lg    0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)
shadow-none  none
```

- **새 그림자 값을 만들지 않는다.** 커스텀 `boxShadow` 토큰을 추가하거나 임의 값(`shadow-[...]`)을 쓰지 않는다. 필요하면 이 표를 먼저 고친다.
- 위 세 개 밖의 단계(`shadow-sm`, `shadow`, `shadow-xl`, `shadow-2xl`, `shadow-inner`)는 이 기준의 어느 역할에도 배정되어 있지 않다. `별도 사용처` 두 곳을 빼면 쓸 자리가 없다.

> **현재 구현과 어긋난 곳 — 후속 작업.** 아래는 코드 확인 결과이며 이 절의 기준이 아니라 **기준에 맞추어야 할 대상**이다. 이 문서만으로 완료 처리하지 않는다.
>
> **`shadow-sm` 17곳은 제거 15 + 교체 1 + 보류 1로 갈린다.** `제거`와 `교체`를 한 덩어리로 세지 않는다.
>
> 1. **제거 15곳.** 콘텐츠 카드 6 + 패널·섹션 컨테이너 9.
>    - 카드 6: 상세 정보 카드 3, `BeforeYouGoCard`, `KoreanInquiryBox`, 관리자 목록의 모바일 장소 카드
>    - 패널·섹션 9: 로그인, `forbidden`, `not-found`, 오류 화면 3(`[locale]`·관리자·`global-error`), 관리자 통계 타일·빈 상태·표 컨테이너
> 2. **교체 1곳.** 지도 위 상태 알림(`MapPanel`)의 `shadow-sm` → `shadow-md`. 지도 위 요소이므로 제거가 아니다.
> 3. **보류 1곳.** 언어 전환 segmented control의 선택 thumb(`shadow-sm`)은 `별도 사용처`다. 현재 값을 유지한다.
>
> 아래는 `shadow-sm` 17곳과 **별개 항목**이다.
>
> 4. 필터 Drawer(`FilterModal`)가 `shadow-2xl`을 쓴다. 떠 있는 레이어이므로 `shadow-lg`로 맞춘다.
> 5. 히어로 장식(`shadow-md`)은 `별도 사용처`이며 현재 값을 유지한다.
> 6. 아직 화면에 연결되지 않은 shadcn 기본 컴포넌트(`ui/dropdown-menu.tsx`)의 최상위 메뉴 표면이 `shadow-md`다. 사용하기 전에 `shadow-lg`로 맞춘다 — 같은 파일의 하위 메뉴는 이미 `shadow-lg`다. 쓰이지 않는 동안에는 화면에 영향이 없으므로 우선순위가 낮다.
>
> **보류 2곳(3·5) 때문에 1·2·4를 미루지 않는다.** 미결정인 것은 그 두 자리의 값뿐이고, 나머지는 이미 확정됐다.

## 5. Layout

### Global Header

```text
[Logo] [주요 탐색 링크] [보조 링크] [EN / KO]
```

- 실제 메뉴 **항목과 경로**는 개발명세서(라우팅)에서 관리한다. 이 문서는 배치·크기·상태만 규정하며, 개발명세서의 디자인 서술은 이 문서를 대체하지 않는다.
- 높이: **64px**. 데스크톱과 모바일이 같다.
- 로고는 홈 링크다.
- 현재 페이지는 텍스트와 indicator로 구분한다.
- 미구현 메뉴는 404로 연결하지 않고 `Coming soon` 또는 비활성 상태로 표시한다.
- 긴급 기능은 위험을 뜻하는 빨간색 장식 대신 명확한 라벨과 아이콘으로 제공한다.

#### Header와 main의 소유 위치

- **사용자 화면은 모두 공통 Header를 가진다.** 목록·지도 같은 탐색 화면뿐 아니라 독립 상세 페이지도 포함한다. 상세 페이지에서 내비게이션이 사라지면 사용자는 뒤로 가기 외에 이동 수단을 잃는다.
- 화면 하나에는 **`<main>` 랜드마크가 정확히 하나** 있어야 한다. skip link의 목적지이자 스크린리더의 본문 진입점이다.
- Header와 `main`은 **한 곳에서만 렌더한다.** 상위 레이아웃이 Header를 그리면 페이지는 그리지 않고, 페이지가 그리면 레이아웃은 그리지 않는다. 어느 쪽이든 프로젝트 안에서 한 방식으로 통일하고, 두 곳에서 동시에 렌더해 중복 landmark가 생기지 않게 한다.
- skip link는 Header 안의 **첫 번째 초점**이며 그 화면의 `main`을 가리킨다(§11).

### Places Desktop

`1280px` 이상에서는 다음 3분할 구조를 사용한다.

```text
[Place list 340px] [Place preview 380px] [Map minmax(0, 1fr)]
```

- 목록과 미리보기 패널은 독립적으로 스크롤한다.
- 지도는 남은 영역을 사용하며 장소 선택 시 재마운트하지 않는다.
- 미선택 시 미리보기 패널은 닫히고 지도가 자연스럽게 확장된다.
- 패널 너비 변화는 `250ms ease-standard`로 처리한다.
- 카드 hover → 마커 hover
- 카드 선택 → 미리보기 패널 + 선택 마커
- 마커 선택 → 해당 카드 scroll into view + 미리보기 패널

`1024–1279px`에서는 목록과 지도를 유지하고 미리보기는 지도 위 overlay panel로 연다. 지도의 실사용 폭이 지나치게 좁아지는 3분할은 사용하지 않는다.

### Places Mobile

`lg` 미만에서는 지도가 화면을 채우고 그 위에 Bottom Sheet가 겹친다.

**Bottom Sheet 3단계**

| 단계 | 목적 | 진입 조건 | 화면 |
|---|---|---|---|
| peek | 결과가 몇 건인지 알리고 목록으로 가는 길을 연다 | 기본 상태 | 결과 수 + 목록/지도 토글만 |
| results | 목록을 훑고 비교한다 | 토글을 눌러 목록을 연다 | 필터 바 · 정렬 · 장소 카드 목록 |
| selected | 고른 장소를 판단한다 | 카드 또는 마커에서 장소를 선택한다 | 미리보기 내용(§6 Place Preview) |

- **시트는 닫히지 않는다.** peek 단계가 항상 떠 있어 결과 수가 보이고, 지도는 그 뒤에서 계속 조작된다. 그래서 이 시트는 모달이 아니다(§11).
- `selected`에서 벗어나는 길을 항상 둔다. 닫기 버튼과 `Escape` 둘 다 이전 단계로 되돌린다.
- 단계 전환은 높이 변화로 표현한다(§8 standard 250ms). 단계를 건너뛰는 전환을 만들지 않는다.

**목록·지도 전환**

- 목록과 지도는 **하나의 토글 컨트롤**로 오간다.
- 라벨은 **누르면 일어날 일**을 쓴다. 지도를 보고 있으면 `목록 보기` / `View list`, 목록을 보고 있으면 `지도 보기` / `View map`. 현재 상태를 라벨로 쓰지 않는다.
- 아이콘만으로 방향을 표시하지 않는다. 텍스트 라벨을 함께 둔다.
- 토글에는 열림 여부를 알리는 상태 속성을 준다(§11).

**공통**

- 필터는 Drawer 또는 Bottom Sheet에서 제공한다.
- 지도와 목록의 필터·선택 상태를 전환 중에도 유지한다.

## 6. Core Components

### Filter Bar

- 카테고리, 실내, 크기, 이동장, 정렬을 중요도 순으로 배치한다.
- 조작 영역은 §6 `Buttons and Inputs`의 규칙을 따른다. 칩이 시각적으로 작아 보여도 실제 조작 영역은 44×44 CSS px 이상이다.
- 비선택: 흰색 배경 + `--color-border-control` 테두리 + `--color-text` 텍스트
- 선택: `primary-soft` 배경 + primary 테두리 + primary 텍스트
- 선택 여부를 테두리 색 하나로만 구분하지 않는다. 배경·테두리·텍스트가 함께 바뀐다.
- 선택된 조건 수가 많아도 필터 바를 여러 줄로 무한 확장하지 않는다.
- 전체 조건은 `All filters`에서 수정한다.

### Place Card

목록 패널에서는 세로형 대형 카드가 아니라 밀도 높은 가로형 카드를 사용한다.

```text
Place name                       620 m
Cafe · Yuseong-gu
[✓ Your dog can visit]
Indoor allowed · Carrier/stroller not required · Up to medium
Checked on July 20, 2026 · Phone
```

- 카드 전체가 선택 대상이며 내부 action의 click propagation을 분리한다.
- 장소명, 거리, 지역, 핵심 조건 3개, 확인 정보가 첫 화면에 보여야 한다.
- 허용 조건만 나열하지 말고 조건부·불가·미확인 상태도 함께 표시한다(§9).
- 카테고리는 텍스트로만 표시한다. 같은 카테고리가 연속으로 나열되는 목록에서 아이콘 뱃지는 폭만 쓰고 구분에 기여하지 않는다.
- hover는 배경색과 테두리로만 표현한다.
- selected는 primary 테두리와 옅은 primary 배경으로 표현한다.
- **핵심 조건 3개와 표시 순서를 유지한다** — 동반 가능 여부 · 이동장·유모차 · 허용 크기. 셋 중 하나가 미확인이어도 항목을 비우지 않는다. 비우면 뒤 조건이 앞자리로 올라와 카드마다 다른 것을 비교하게 된다.
- **`핵심 조건 3개`는 항목 수이지 줄 수가 아니다.** 한 항목이 여러 줄로 늘어날 수 있고, 동반 가능 여부 항목은 구역 기록을 함께 담아 여러 줄이 되기도 한다.
- 조건 영역은 **공통 최소 높이로 기본 정렬을 맞추고, 긴 문구는 줄바꿈에 따라 필요한 만큼 확장한다.** 모든 카드의 동일 높이보다 **조건 정보의 가독성과 완전성을 우선한다.**
- **폭이 부족하다는 이유로 문구를 줄이지 않는다.** 줄바꿈하거나 조건 행의 폭을 늘린다(§9 `조건별 주의`).
- 대표 이미지는 카드에 넣지 않는다. 340px 목록에서 이미지는 조건 텍스트를 밀어내며, 조건이 판단의 근거다.
- 홈과 즐겨찾기 grid에서는 4:3 이미지의 세로형 variant를 사용할 수 있다.

#### Visit eligibility banner

반려견 프로필이 등록되어 있으면 조건을 나열하기 전에 방문 가능 여부를 한 문장으로 단언한다. 사용자가 조건 여러 개를 직접 종합하게 두지 않는다.

| 상태 | 조건 | 표현 |
|---|---|---|
| allowed | 허용 크기 ≥ 등록된 반려견 크기 | `success-soft` 배경 + check 아이콘 |
| blocked | 동반 불가이거나 허용 크기 미만 | `danger-soft` 배경 + slash 아이콘 |
| unknown | 허용 크기 미확인 | `unknown-soft` 배경 + alert 아이콘 |

- 프로필이 없으면 배너를 표시하지 않는다. 추측으로 단언하지 않는다.
- 배너는 판정만 담고, 근거가 되는 조건은 바로 아래 조건 목록에서 그대로 보여준다.

#### Density verification

- 340px 목록 기준, 영문 장소명 2줄 + 조건 3개 + 확인 정보가 잘리지 않는지 검증한다.
- 공간이 부족하면 조건 값을 축약형으로 표시한다. (예: `Maximum dog size: Medium` → `Up to medium`) 축약형도 의미를 생략하지 않는다. 정식 문구와 축약형은 §9 `조건 상태 어휘`가 정한다.
- **모든 조건이 축약으로 짧아지지는 않는다.** 카드에는 조건 이름이 없어서 이동장·유모차 조건은 `Carrier/stroller not required`보다 짧게 쓸 수 없고, `실내 불가 · 야외 미확인` 상태는 두 사실을 함께 담아야 한다(§9). 이런 조건은 축약 대신 줄바꿈이나 폭 확보로 처리한다.

**검증 방법** — 두 가지를 구분해서 기록한다.

| 단계 | 방법 | 결과 표기 |
|---|---|---|
| 실데이터 검증 | 현재 공개된 장소 중 **가장 긴 이름·주소**로 확인한다. 데이터가 적어도 이 검증은 항상 가능하다 | `실데이터 확인` |
| 한계 검증 | 더 긴 이름·주소·영문 문장을 **가상 데이터**로 넣어 확인한다 | `가상 데이터 확인` |

- 두 결과를 섞어 적지 않는다. 가상 데이터로만 확인한 것을 실제로 확인했다고 쓰지 않는다.
- 데이터가 적은 것과 검증이 불가능한 것은 다르다. 실데이터가 적으면 **범위가 좁다**고 적고, 검증 자체를 건너뛰지 않는다.
- 텍스트 200% 확대와 좁은 폭에서의 reflow는 **별도 검증 항목**이다. 밀도 검증이 통과했다고 함께 통과한 것으로 보지 않는다.

### Place Preview

목록이나 지도에서 장소를 선택했을 때 여는 요약이다. 전체 정보를 담지 않는다. 갈 수 있는지 판단할 최소 정보와 다음 행동만 제공한다.

구조는 header, body, footer 세 영역으로 고정한다. header와 footer는 고정하고 body만 스크롤한다.

```text
header   카테고리 아이콘 · 장소명 · 카테고리 · 거리 · 주소 · 닫기
body     동반 조건 전체 · 주의사항 · 확인일과 확인 방법
footer   [즐겨찾기] [길찾기] [상세 보기]
```

footer는 **한 줄에 최대 3개**를 둔다. 왼쪽부터 즐겨찾기(아이콘), 길찾기(secondary), 상세 보기(primary) 순이다.

- 길찾기는 좌표가 없으면 **버튼 자체를 생략한다.** 비활성 버튼을 남기지 않는다(§6 Place Detail Page와 같은 규칙).
- 좁은 화면과 긴 번역에서도 **버튼 라벨을 자르지 않는다.** 폭이 부족하면 아이콘 버튼의 여백을 먼저 줄이고, 그래도 부족하면 secondary 라벨을 축약형으로 바꾼다. 말줄임표로 자르거나 라벨을 지우지 않는다.
- 어떤 조합에서도 primary는 정확히 하나다.

- **미리보기는 카드보다 반드시 많은 정보를 보여준다.** 카드와 같은 내용만 반복하면 패널을 열 이유가 없다.
- 카드는 핵심 조건 3개(실내 동반, 이동장·유모차, 허용 크기)만, 미리보기는 목줄과 입마개까지 포함한 전체 조건을 세로로 나열한다.
- 카드는 주소를 한 줄로 자르고, 미리보기는 전체 주소를 보여준다.
- 주의사항이 있으면 `warning-soft` 박스로 조건 아래에 둔다. 없으면 영역 자체를 생략한다.
- 확인일에는 확인 방법을 함께 표시한다. 마지막 확인이 `90일` 이상 지났을 때만 `Recheck needed`를 경고색으로 알린다. 임계값을 낮게 잡아 모든 카드가 경고색이 되면 경고가 아무것도 강조하지 못한다. **임계값은 90일 하나만 사용한다.** 중간 단계 경고를 추가하지 않는다.
- `상세 보기`가 primary action이다. 미리보기의 목적은 상세로 이어주는 것이다.
- 대표 이미지, 운영시간, 연락처, 문의 문구 복사는 미리보기에 넣지 않는다.
- 표면은 breakpoint에 따라 컬럼, overlay panel, Bottom Sheet로 바뀌지만 내용과 순서는 동일하게 유지한다.

### Place Detail Page

`/places/{id}`. 장소의 모든 정보를 담는 최종 화면이다. 여기서 더 들어갈 곳은 없다.

표시 순서:

1. 대표 이미지
2. 장소명, 카테고리, 지역, 거리
3. 길찾기·전화·공유 action
4. `Before You Go`
5. 확인일·확인 방법·디스클레이머
6. 운영시간과 연락처
7. `Ask the store in Korean` — 영어 UI만 *(MVP 제외 / P1 재도입 예정)*
8. 신고

- `Before You Go`를 이미지 바로 아래의 핵심 영역에 둔다.
- **확인 정보(확인일·확인 방법)와 디스클레이머는 `Before You Go` 바로 다음, 운영시간·연락처보다 앞에 온다.** 조건을 읽은 직후에 그 조건을 얼마나 믿을 수 있는지 알려주는 것이 §3.3의 요구다. 운영시간은 방문을 정한 뒤에 필요한 정보라 뒤에 둔다.
- 길찾기를 기본 primary action으로 사용하고, 전화와 문의 문구 복사는 secondary action으로 둔다.
- 전화번호나 링크가 없으면 비활성 버튼을 노출하지 말고 해당 action을 생략한다.
- 이 화면도 공통 Header와 `main` 랜드마크를 갖는다(§5 `Header와 main의 소유 위치`).

### Condition Row

```text
[icon] Indoor access       Indoor access allowed
[icon] Carrier / stroller  Carrier or stroller required
[icon] Dog size limit      Size limit unconfirmed
```

- label과 value를 분리해 빠르게 스캔할 수 있게 한다.
- 값은 §9 `조건 상태 어휘`의 **정식 문구**를 쓴다. 이 행은 폭이 넉넉하므로 축약형을 쓰지 않는다.
- 모든 값을 초록색 badge로 만들지 않는다.

### Verification Status

```text
Checked 3 days ago · Confirmed by phone
Checked on July 20, 2026 · Recheck needed
Indoor access unconfirmed
```

- `Verified`라는 단어만 표시하지 않는다.
- 확인일과 확인 방법을 함께 노출한다.
- 확인일 표기 형식은 §10 `확인일 표기`를 따른다.
- 오래된 정보는 Amber, 미확인 정보는 Gray를 사용한다. 이 둘은 서로 다른 상태다(§9).
- 검증 정보가 없으면 날짜나 방법을 추정하지 않는다.

### Korean Inquiry Box

> ⛔ **MVP 제외 (P1 재도입 예정).** 기획서 v3 §11 결정에 따라 MVP에서는 이 컴포넌트를 사용자 화면에 노출하지 않는다. 컴포넌트 코드와 메시지 키는 P1의 동적 문의 문구 기능을 위해 보존한다.
> 아래 규칙은 **재도입 시점의 기준**으로 유지한다. 지금 이 규정을 근거로 화면에 박스를 추가하지 않는다.

- 영어 UI에서만 노출한다. 한국어 UI에서는 박스 전체를 생략하고 전화 action만 유지한다. 빈 자리를 다른 콘텐츠로 채우지 않고 다음 섹션을 자연스럽게 올린다.
- 한국어 원문을 수정 가능한 것처럼 보이게 하지 않는다.
- 안내 문구, 한국어 메시지, `Copy Korean message` 버튼 순서로 구성한다.
- 복사 성공은 `Copied to clipboard` Snackbar로 알린다.
- 사용자가 직접 보내도록 하며 자동 전송처럼 표현하지 않는다.

### Map and Markers

- 기본 마커: 흰색 surface + 회색 border
- hover 마커: primary ring
- 선택 마커: primary 배경 + 흰색 아이콘
- 카드와 마커의 hover·selected 상태를 시각적으로 구분한다.
- 지도 위 컨트롤은 우측 또는 하단 한 영역에 모아 배치한다.
- 검색 결과가 바뀌어도 지도를 불필요하게 초기화하지 않는다.
- 선택 장소를 보여주기 위해 과도하게 zoom하지 않는다.

### Buttons and Inputs

- Primary: 현재 맥락의 대표 행동 하나
- Secondary: outline. 테두리는 `--color-border-control`
- Tertiary: ghost 또는 text
- Destructive: 삭제·신고 확정 등 실제 위험 행동에만 사용
- disabled 상태에서도 크기와 배치가 바뀌면 안 된다.

#### 크기와 조작 영역

**보이는 크기**와 **실제로 눌리는 영역**은 다른 값이다. 둘을 구분해서 정한다.

| 항목 | 규칙 |
|---|---|
| 조작 영역 | 사용자 화면의 버튼·입력·칩·아이콘 버튼은 **최소 44×44 CSS px** |
| 보이는 크기 | 조작 영역보다 작아도 된다. 칩처럼 작게 보여야 하는 요소는 여백이나 투명 영역으로 44px을 채운다 |
| 입력 높이 | 보이는 높이 44–48px. 조작 영역을 따로 넓히지 않아도 되는 크기다 |
| 아이콘 단독 버튼 | 조작 영역 44×44px, `aria-label` 필수 |

- 44×44px은 **이 프로젝트가 정한 규칙**이다. WCAG 2.1 AA가 요구하는 값이 아니다(§11).
- 조작 영역을 넓힐 때 **인접 컨트롤의 영역과 겹치면 안 된다.** 겹치면 사용자가 의도한 것과 다른 컨트롤이 눌린다. 컨트롤 사이 간격이 부족하면 영역을 넓히는 대신 간격을 늘린다.
- 조작 영역은 화면 폭과 무관하게 적용한다. 데스크톱이라고 줄이지 않는다.
- **예외**: 관리자 화면의 조밀한 표 내부 컨트롤에 한해 40px을 허용한다. 범위는 §6 Admin이 정한다.

### Admin

- 사용자용 카드 스타일을 억지로 재사용하지 않는다.
- 표, 폼, 상태 필터 중심의 높은 정보 밀도를 허용한다.
- 장소 조건과 검증 항목은 섹션으로 구분하고 저장 오류는 해당 필드 근처에 표시한다.
- 상태 색상 의미는 사용자 화면과 동일하게 유지한다.

**40px 예외의 범위**

- **데스크톱 폭에서, 표 안에 들어가는 행 단위 컨트롤**(행 액션 버튼, 인라인 상태 토글 등)에만 40px을 허용한다.
- 관리자 화면 전체가 예외인 것은 아니다. 폼 입력·주요 버튼·모달 안의 컨트롤은 사용자 화면과 같은 44px 규칙을 따른다.
- 터치 환경과 좁은 폭에서는 예외를 적용하지 않는다. 관리자도 태블릿·모바일에서 열 수 있다.
- 대비·포커스 표시·`aria` 규칙에는 예외가 없다.

## 7. States

| State | Treatment |
|---|---|
| Initial loading | 자리를 대신할 요소와 **같은 구조**의 Skeleton. 카드 자리는 최종 카드 구조를, 패널 자리는 그 패널 구조를 따른다. 크기·배치·radius 모두 대상 요소의 값을 쓴다(§4 Radius) |
| Load more (페이징 도입 시) | 기존 목록을 유지하고 하단 spinner 표시. **현재는 결과를 한 번에 모두 불러오므로 이 상태가 화면에 없다.** 페이징을 도입하면 이 규칙을 따른다 |
| No results (필터) | `No places match your filters.` + `Reset filters` |
| No places yet | `No places are available yet.` — 지울 필터가 없으므로 액션을 두지 않는다 |
| Location denied | `Showing results near Daejeon center.` + `Near me`. 거리는 표시하지 않고 지도 중심만 대전으로 둔다 |
| Out of service area (D-11) | Amber 배너 `We currently cover Daejeon only.` + `View places in Daejeon`. 사용자가 허용한 위치를 말없이 무시하지 않고, 이동 여부를 버튼으로 맡긴다 |
| Map error | 목록은 유지하고 지도 영역에 재시도 안내 |
| Stale verification (마지막 확인일로부터 90일 이상 경과) | Amber 아이콘 + `Recheck needed` + 마지막 확인일 + 정책 변경 가능 문구 |
| Unknown condition | Gray 아이콘 + 해당 조건의 미확인 문구 (§9). 예: `Indoor access unconfirmed` |
| Dog mismatch | 조건 불일치 항목을 명시하고 카드 전체를 error처럼 만들지 않음 |
| Copy success | 짧은 Snackbar, 3초 이내 자동 종료 |
| Network error | 원인 범위를 설명하는 한 문장 + `Try again` |
| Unauthenticated favorite | 현재 맥락을 보존한 로그인 안내 |

- 빈 상태에 장식용 일러스트를 기본으로 넣지 않는다.
- 로딩 중 기존 지도와 장소 목록을 불필요하게 가리지 않는다.
- 오류는 사용자 탓으로 표현하지 않는다.
- 표의 상태 중 **아직 화면에 없는 것**은 그 사실을 행에 적는다. 지금 구현되지 않았다는 이유로 유효한 설계 요구를 표에서 지우지 않는다. 반대로, 표에 있다는 이유만으로 그 상태가 구현되어 있다고 읽지 않는다.

## 8. Motion

```text
instant   0ms    checkbox, toggle
fast    150ms    hover, focus, button press
standard 250ms   panel, tab, Bottom Sheet
slow    350ms    큰 상태 전환
```

```css
--ease-standard: cubic-bezier(0.4, 0, 0.2, 1);
--ease-enter: cubic-bezier(0, 0, 0.2, 1);
--ease-exit: cubic-bezier(0.4, 0, 1, 1);
```

- `instant`는 전환을 두지 않는다는 뜻이다. 지속시간 토큰이 따로 있지 않다.
- bounce와 overshoot를 사용하지 않는다.
- 지도 마커와 카드 강조는 150ms 이내로 반응한다.

### Reduced motion

`prefers-reduced-motion: reduce`에서의 처리를 효과별로 나눈다.

| 효과 | 처리 |
|---|---|
| 위치 이동 (slide, translate, 시트·패널 열림, 스크롤 애니메이션) | **제거한다.** 최종 위치로 즉시 놓는다 |
| 크기 변화 (scale, 확대·축소 전환) | **제거한다.** 최종 크기로 즉시 놓는다 |
| 회전 (아이콘 방향 전환 등) | **제거한다.** 최종 각도로 즉시 놓는다 |
| 색·투명도 변화 (hover, focus, 배경색 전환, 페이드) | **남긴다.** 화면 위치가 움직이지 않고, 상태 변화를 알리는 데 필요하다 |

- 제거는 "느리게"가 아니라 **즉시 최종 상태**를 뜻한다. 지속시간을 늘리지 않는다.
- reduced motion을 위해 **새 지속시간이나 새 효과를 만들지 않는다.** 위 표는 기존 효과를 끄고 켜는 규칙일 뿐이다.
- 모션을 제거해도 요소가 나타나고 사라지는 **순서와 최종 배치는 그대로**여야 한다.

## 9. Voice and Content

- 영어 문장은 짧고 직접적으로 작성한다.
- 감성적 여행 표현보다 조건의 정확성을 우선한다.
- 지역명, 거리, 날짜, 확인 방법을 구체적으로 쓴다.
- 확인하지 않은 내용을 긍정적으로 추정하지 않는다.

권장:

```text
Indoor access allowed
Outdoor area only
Carrier or stroller required
Size limit unconfirmed
Checked by phone on July 20, 2026
Store policies may change
```

위 문구는 §9 `조건 상태 어휘`의 정식 문구와 같다. 조건 상태를 나타내는 문장은 그 표에서 가져오고, 여기서 새로 만들지 않는다.

금지:

```text
Perfect for every dog
Completely pet-friendly
Dogs are always welcome
Guaranteed entry
Amazing place
Oops, something went wrong
```

### 조건 상태 어휘

같은 상태는 카드·미리보기·상세에서 **같은 말로** 읽혀야 한다. 화면마다 길이는 달라도 되지만 뜻이 달라 보이면 안 된다.

**상태 구분** — 아래 셋은 서로 다른 상태다. 한 단어로 뭉뚱그리지 않는다.

| 상태 | 뜻 | 색 |
|---|---|---|
| 불가 (`not allowed`) | 확인했고, 안 된다 | Red |
| 미확인 (`unconfirmed`) | 그 조건을 확인한 적이 없다 | Gray |
| 재확인 필요 (`recheck needed`) | 확인은 했지만 마지막 확인일로부터 90일 이상 지났다. **조건 값 자체는 그대로 두고 신선도만 낮다** | Amber |

**정식 문구와 축약형**

정식 문구는 미리보기·상세에서 쓴다. 축약형은 카드처럼 폭이 좁은 곳에서만 쓰며, 뜻을 줄이지 않는다(§6 Density verification).

| 조건 | 상태 | 정식 (EN) | 정식 (KO) | 축약형 (EN / KO) |
|---|---|---|---|---|
| 실내 동반 (`indoor`) | 실내 가능 | `Indoor access allowed` | `실내 동반 가능` | `Indoor allowed` / `실내 가능` |
| | 실외만 | `Outdoor area only` | `실외·테라스만 가능` | `Outdoor only` / `실외만 가능` |
| | 일부 구역 | `Partial indoor area` | `일부 구역만 가능` | `Partial area` / `일부 구역` |
| | 전체 동반 불가 | `Dogs not allowed` | `반려견 동반 불가` | `Dogs not allowed` / `동반 불가` |
| | 미확인 | `Indoor access unconfirmed` | `실내 동반 미확인` | `Indoor unconfirmed` / `실내 미확인` |
| 이동장·유모차 | 불필요 | `Carrier or stroller not required` | `이동장·유모차 불필요` | 문맥에 따라 갈린다 (아래 `조건별 주의`) |
| | 실내에서 필요 | `Carrier, stroller, or holding required indoors` | `실내에서는 이동장, 유모차, 안기 중 하나가 필요` | 축약하지 않는다 (아래 `조건별 주의`) |
| | 항상 필요 | `Carrier or stroller required` | `이동장 또는 유모차 필수` | 축약하지 않는다 (아래 `조건별 주의`) |
| | 미확인 | `Carrier or stroller requirement unconfirmed` | `이동장·유모차 필요 여부 미확인` | `Requirement unconfirmed` / `필요 여부 미확인` |
| 최대 허용 크기 | 소형견 | `Maximum dog size: Small` | `최대 허용 크기: 소형견` | `Up to small` / `소형견까지` |
| | 중형견 | `Maximum dog size: Medium` | `최대 허용 크기: 중형견` | `Up to medium` / `중형견까지` |
| | 대형견 | `Maximum dog size: Large` | `최대 허용 크기: 대형견` | `Up to large` / `대형견까지` |
| | 미확인 | `Maximum dog size unconfirmed` | `최대 허용 크기 미확인` | `Size limit unconfirmed` / `크기 상한 미확인` |
| 신선도 | 재확인 필요 | `Recheck needed` | `재확인 필요` | 같음 |

**조건별 주의**

- **이동장·유모차 조건은 `무엇이 필요한가`와 `무엇으로 대신할 수 있는가`를 같이 말한다.** 축약할 때 한쪽을 빼지 않는다. `이동장 필수`처럼 한쪽만 남기면 유모차로 대체할 수 있다는 정보가 사라진다.
- **`이동장·유모차 필수`는 쓰지 않는다.** 가운뎃점은 `둘 다 필요하다`로 읽힌다. 아래 셋은 서로 다른 상태이며 같은 문구로 쓰지 않는다.

  | 실제 정책 | 문구 |
  |---|---|
  | 둘 중 하나면 된다 | `이동장 또는 유모차 필수` / `Carrier or stroller required` |
  | 둘 다 있어야 한다 | 둘 다 필요하다고 명시한다. `또는`으로 쓰지 않는다 |
  | 한 종류만 허용한다 | 허용되는 것만 적는다. 예: `이동장만 허용` |

- **`No carrier/stroller`는 쓰지 않는다.** `필수가 아니다`라는 뜻이 사라지고 `이동장·유모차가 없다` 또는 `반입할 수 없다`로 읽힌다. `불필요` 상태의 축약형은 문맥에 따라 둘로 갈린다.

  | 문맥 | 문구 |
  |---|---|
  | 값만 보이고 조건 이름이 없다 | `Carrier/stroller not required` / `이동장·유모차 필수 아님` |
  | `이동장·유모차` 라벨이 값에 붙어 있다 | `Not required` / `필수 아님` |

  - **카드는 첫 번째 행이다.** 현재 카드는 상태 아이콘과 값만 보여주고 조건 이름을 붙이지 않는다. 라벨이 생기기 전에는 `Not required`를 쓰지 않는다.
  - 첫 번째 행은 정식 문구보다 두 글자밖에 짧지 않다. **이 조건은 축약으로 밀도 문제를 풀 수 없다** — 폭이 부족하면 줄바꿈하거나 조건 행의 폭을 늘린다.

- **`필수 아님`은 사용 의무만 부정한다.** 이동장·유모차를 반입할 수 없다는 뜻도, 써도 된다는 뜻도 아니다. 반입 금지나 사용 허용은 확인된 근거가 있을 때 `policyDetails`에 따로 담고, 이 문구에서 추정하지 않는다.

- **필드명으로 대체 가능 여부를 추정하지 않는다.** `carrierStrollerPolicy`라는 이름은 두 물건을 한 컬럼에 담고 있을 뿐, 둘이 서로 대신할 수 있다는 뜻이 아니다. 대체 가능 여부는 확인된 안내문 근거로만 정한다.
- **컬럼은 셋을 구분하지 못한다.** `REQUIRED_ALWAYS`는 `필요하다`는 사실만 담는 보수적 요약값이다. 셋을 구분해 담는 자리는 `policyDetails`의 준비물 그룹이다 — 그룹 모드 `ANY_OF`가 `둘 중 하나`, `ALL_OF`가 `둘 다`이고, 한 종류만 허용하는 경우는 그 항목 하나만 `REQUIRED`로 둔다. 근거가 그 그룹에 없으면 컬럼 값만으로 `또는`이라고 단정하지 않는다.
- **대체 관계를 담은 두 행(`실내에서 필요`, `항상 필요`)은 축약형을 쓰지 않는다.** 어느 축약도 `무엇으로 대신할 수 있는가`를 지우기 때문이다. `불필요`와 `미확인` 행은 대체 관계를 말하지 않으므로 축약형을 쓸 수 있다 — 다만 `불필요`의 축약형은 위 문맥 표를 따른다. 이전 판의 `실내 안기·유모차`는 이동장이 빠져 있었고, `이동장·유모차 필수`는 둘 다로 읽혔다. 폭이 부족하면 줄바꿈하거나 조건 행의 폭을 늘린다(§10 `버튼 텍스트가 길어지면...`과 같은 원칙).
- **미확인은 확인 대상을 밝힌다.** `이동장 미확인`은 이동장이라는 물건을 확인하지 못했다는 뜻으로도 읽힌다. 확인하지 못한 것은 **필요 여부**다.
- **최대 허용 크기는 상한이다.** `대형견 가능`은 대형견만 되는지 대형견까지 되는지 모호하다. 정식 문구는 `최대 허용 크기: 대형견` 형식을 쓰고, 축약형도 `대형견까지`처럼 상한임이 드러나야 한다.
- `Large allowed`처럼 **단독으로 뜻이 완성되지 않는 문구**는 쓰지 않는다.
- **실외만 가능(`OUTDOOR_ONLY`)은 야외 동반이 가능하다는 정보**를 담는다. 실내가 안 된다는 사실만 전달하는 표현으로 바꾸지 않는다.

#### 실내 동반 불가와 전체 동반 불가

이 둘은 **서로 다른 상태**다.

- **실내 동반 불가**: 실내에 들어갈 수 없다. 야외 동반 가능 여부는 이 사실만으로 알 수 없다.
- **전체 동반 불가**: 그 장소에 반려견을 데려갈 수 없다. `Dogs not allowed` / `반려견 동반 불가`.

**현재 모델에서 `indoor`의 `NOT_ALLOWED`는 전체 동반 불가다.** 실내만 불가하다는 뜻이 아니다.

**근거 — 이 의미는 문서보다 먼저 세 층에서 쓰이고 있다.**

| 층 | 어디 | 이 값을 어떻게 쓰는가 |
|---|---|---|
| 관리자 입력 | 장소 등록·수정 폼의 실내 조건 선택지 | `NOT_ALLOWED`의 라벨이 `동반 불가`다. `실내 불가`가 아니다 |
| 매칭 판정 | 반려견 매칭 | `not_allowed`이면 크기·견종을 보기 전에 `PET_NOT_ALLOWED`로 확정한다. 실내만 불가한 경우는 `OUTDOOR_ONLY`·`PARTIAL_AREA`가 따로 맡는다 |
| 방문 가능 판정 | 방문 가능 배너와 미리보기 상태 | `not_allowed`이면 등록된 반려견 크기와 무관하게 `blocked` / `방문 불가`다. §6 `Visit eligibility banner`의 `blocked` 조건이 이미 `동반 불가이거나 허용 크기 미만`으로 적혀 있다 |
| 테스트 | 매칭 테스트 | `동반 불가 장소는 모든 반려견에 대해 PET_NOT_ALLOWED가 최우선이다`가 고정되어 있다 |

- 위는 **현재 모델의 정의**를 확인한 것이다. 이미 저장된 장소들이 어떤 의도로 입력됐는지 확인한 결과가 아니다. 저장된 사례를 점검하지 않은 채 `과거 입력도 이 뜻이었다`고 말하지 않는다. 기존 데이터 점검은 별도 작업이다.

**필드명이 실제 의미 범위보다 좁다.**

`indoor` / `IndoorPolicy`라는 이름은 실내 여부만 담는 것처럼 읽힌다. 하지만 이 필드는 세 가지를 함께 표현한다.

1. **실내 동반 가능 여부** — `ALLOWED`
2. **동반 가능한 구역** — `OUTDOOR_ONLY`, `PARTIAL_AREA`
3. **장소 전체 동반 가능 여부** — `NOT_ALLOWED`

- 이름만 보고 `NOT_ALLOWED`를 `실내 불가`로 읽지 않는다. 화면 문구는 필드명이 아니라 아래 표시 기준을 따른다.
- 필드명을 바꾸는 것은 마이그레이션이 필요한 별도 작업이다. 이 문서는 이름을 바꾸지 않고 뜻을 고정한다.

**표시 기준**

| 확인된 사실 | 값 | 표시 |
|---|---|---|
| 그 장소에 반려견을 데려갈 수 없다 | `NOT_ALLOWED` | `Dogs not allowed` / `반려견 동반 불가` |
| 야외·테라스는 되고 실내는 안 된다 | `OUTDOOR_ONLY` | `Outdoor area only` / `실외·테라스만 가능` — 야외 가능과 실내 불가를 **함께** 담는다 |
| 실내가 안 되는 것만 확인됐고 야외 이용 여부는 모른다 | `UNKNOWN` + `policyDetails` (아래 `저장 구조`) | `No indoor access · Outdoor access unconfirmed` / `실내 동반 불가 · 야외 동반 미확인` — 두 사실을 **함께** 전달한다 |

규칙:

1. 실내 동반 불가라는 정보만으로 **야외 동반이 가능하다고 추정하지 않는다.** 야외 가능은 `OUTDOOR_ONLY`가 따로 표현한다.
2. 실내 동반 불가라는 정보만으로 **전체 동반 불가라고 추정하지 않는다.** `NOT_ALLOWED`는 전체 동반 불가를 확인했을 때 쓰는 값이다.
3. **전체 동반 불가는 그 사실을 확인할 근거가 있을 때만 표현한다.** 근거가 없으면 확인된 범위까지만 말한다.
4. 두 상태를 같은 문구로 쓰지 않는다.

**실내 불가 · 야외 미확인의 표시 기준**

이 경우는 **두 사실을 함께** 말한다. 하나만 말하면 나머지가 추정된다.

1. 실내 동반 불가 — 확인된 사실
2. 야외 동반 가능 여부 미확인 — 확인되지 않은 사실

| 언어 | 문구 |
|---|---|
| EN | `No indoor access · Outdoor access unconfirmed` |
| KO | `실내 동반 불가 · 야외 동반 미확인` |

- **조건의 미확인은 `미확인`으로 적는다.** 신선도의 `재확인 필요`(90일 경과, Amber), 방문 상태 제목의 `방문 전 확인 필요`, 그리고 `매장에 문의` 같은 행동 안내와 각각 다른 것이다. 이 상태의 색은 미확인의 Gray다.
- 이 문구는 **야외에 확인된 출입 정보가 없을 때만** 쓴다. 야외·테라스에 확인된 기록이 있으면 그 구역과 적용 대상을 그대로 보여준다(아래 `구역 기록`).
- 아래 셋으로 **표시하지 않는다.**

  | 쓰지 않는 표시 | 이유 |
  |---|---|
  | `실내 동반 미확인` / `Indoor access unconfirmed` | 실내는 확인됐다. 모르는 것은 야외다 |
  | `반려견 동반 불가` / `Dogs not allowed` | 전체 동반 불가를 확인한 적이 없다 |
  | `실외·테라스만 가능` / `Outdoor area only` | 야외 가능을 확인한 적이 없다 |

- **방문 가능 여부를 확정해 안내하지 않는다.** 동반 가능한 구역이 확인되지 않았으므로 `방문 가능`도 `방문 불가`도 단언하지 않는다. §6 `Visit eligibility banner`의 `unknown`이 이 경우를 받는다.
- **카드·미리보기·상세가 같은 의미를 전달한다.** 길이는 달라도 되지만, 세 화면 중 하나만 `실내 불가`를 빠뜨리거나 하나만 야외를 단정하면 안 된다.

  | 화면 | 최소 조건 |
  |---|---|
  | 카드 | 두 사실을 함께 보여준다. 폭이 부족하면 실내 조건 한 줄을 두 줄로 늘린다 |
  | 미리보기 | 조건 목록의 실내 항목에서 두 사실을 함께 보여주고, 상태 영역은 `방문 전 확인 필요`로 둔다 |
  | 상세 | 조건 행에 두 사실을 적고, 근거가 되는 구역 단위 기록을 `policyDetails` 영역에 그대로 보여준다 |

**구역 기록 — 구역과 적용 대상을 보존한다**

`policyDetails`의 구역 기록을 요약 한 줄로 뭉개지 않는다.

- `access`가 `UNKNOWN`인 기록은 **허용의 근거로도 불가의 근거로도 쓰지 않는다.** 아무 말도 하지 않는 값이다.
- 출입 여부는 `spaceExceptions`에서만 읽는다. 다른 정책의 적용 범위(`handling`의 `scope` 등)로 출입 허용·불가를 **추론하지 않는다.**
- 테라스 기록을 전체 야외로, 특정 크기 기록을 전체 반려견으로 **넓히지 않는다.** `대형견만 실내 불가`는 대형견 이야기다.
- 실내와 야외 양쪽에 불가 기록이 있어도 **장소 전체 `NOT_ALLOWED`로 바꾸지 않는다.** 기록된 구역의 제한으로 표시한다. `NOT_ALLOWED`는 요약 컬럼이 그렇게 확인됐을 때만 쓰는 값이다.
- 요약 문구가 이미 말한 사실은 구역 기록으로 **한 번 더 적지 않는다.** 겹치는 것만 빼고 구역·대상이 다른 기록은 남긴다.

**명백한 정보 불일치**

요약 컬럼과 구역 기록이 같은 범위를 두고 서로 반대로 말할 때다.

| 항목 | 규칙 |
|---|---|
| 방문 판정 | `unknown`. **어느 쪽도 사실로 확정하지 않는다** — 방문 가능도 방문 불가도 단언하지 않는다 |
| 조건 요약 | `Conflicting dog access information` / `동반 조건 정보 불일치` |
| 상세 | 어떤 안내가 서로 어긋나는지 보여주고, 매장 확인 안내는 **별도 문장**으로 둔다 |
| 카드 | 서로 모순되는 내용을 각각 확정된 사실처럼 나열하지 않는다 |

- **정상적인 예외를 충돌로 보지 않는다.** 전체 반려견(`appliesToSize: ALL`)에 적용되는 기록만 요약과 충돌할 수 있다. `대형견만 실내 불가`, `2층만 불가` 같은 크기·세부 구역 예외는 정상적인 표현이다.
- 저장 단계에서 이런 모순을 막을지는 **별도 후속 작업**이다(§13 G-1).

**저장 구조 — 새 값을 만들지 않는다**

`indoor`에는 `실내 불가, 야외 불명`을 뜻하는 값이 없다. **새 enum 값이나 새 필드를 임의로 만들지 않는다.** 기존 구조 안에서 다음과 같이 담는다.

| 자리 | 값 | 뜻 |
|---|---|---|
| `indoor` 컬럼 | `UNKNOWN` | 동반 가능한 구역이 확인되지 않았다 |
| `policyDetails.spaceExceptions[]` | `{ area: "INDOOR", appliesToSize: "ALL", access: "NOT_ALLOWED" }` | 실내에 대해서만 말한다. 야외에 대해서는 아무 말도 하지 않는다 |

- 위 식별자는 **실제 스키마·검증과 대조했다** — `IndoorPolicy`에 `UNKNOWN`, `SPACE_AREAS`에 `INDOOR`, `SPACE_ACCESS`에 `NOT_ALLOWED`, `SIZE_SCOPES`에 `ALL`이 있다. `spaceExceptions`는 최대 8개이며, 스키마 검증은 값을 다시 쓰지 않고 통과·거부만 한다.
- `indoor`를 `UNKNOWN`으로 두는 것은 규칙 1·2를 지키기 위해서다. `OUTDOOR_ONLY`로도 `NOT_ALLOWED`로도 올리지 않는다.
- 문장이 더 필요하면 주의사항 텍스트에 남긴다.

> **제약 — 저장할 수 있는 것과 화면이 보여주는 것은 다르다.**
>
> 위 구조는 스키마가 허용하고 관리자 화면에서 입력할 수 있다. 그러나 이 값을 읽어 사용자 화면에 보여주는 곳은 **상세 하나뿐**이다.
> - 카드와 미리보기는 `indoor` 요약 컬럼만 읽는다. 이 상태의 장소는 두 화면에서 `실내 동반 미확인`으로만 보이고, 확인된 `실내 불가`가 사라진다. 위 표시 기준에 어긋나므로 **후속 수정 대상**이다.
> - **저장 경로가 있다는 이유로 이 기능을 구현된 것으로 처리하지 않는다.** 표시 경로가 없다.

> **현재 구현과 어긋난 곳 — 후속 작업.** 아래는 코드 확인 결과이며, 이 절의 기준이 아니라 **기준에 맞추어야 할 대상**이다. 문서 정비를 이 항목들의 완료로 읽지 않는다.
>
> 1. 미리보기 계열 메시지의 `실내 입장 불가` / `Indoor seating not allowed`가 이 정의와 어긋난다. `반려견 동반 불가` / `Dogs not allowed`로 고친다. (이 키 묶음은 현재 어느 컴포넌트도 참조하지 않는다 — 살릴 때 함께 고치거나, 쓰지 않기로 하면 지운다.)
> 2. 상세 `Before You Go`의 실내 조건 행은 라벨이 `실내 동반` / `Indoor access`인데 `NOT_ALLOWED`의 값이 `동반 불가` / `Dogs not allowed`다. 라벨과 값의 범위가 어긋난다. 이 값일 때는 조건 행 하나로 처리하지 않고 장소 전체 상태로 보여주는 방법을 정한다.
> 3. 미리보기가 `NOT_ALLOWED`를 `조건이 있어요` / `Conditions` 목록의 한 줄로 넣는다. 전체 동반 불가는 지켜야 할 조건이 아니라 방문 불가 판정이다. 목록 대신 상태 영역에서 말한다.
> 4. 관리자 표의 영어 라벨이 `Not allowed`다. 무엇이 불가한지가 문구에 없다. 관리자 화면은 §9의 사용자 어휘 대상이 아니지만, 같은 값을 두 화면이 다르게 부르지 않도록 함께 본다.
> 5. **카드와 미리보기가 `indoor` 요약 컬럼만 읽는다.** `실내 불가 · 야외 미확인` 상태를 두 화면이 표현하지 못한다(위 `제약`). 두 층을 함께 읽는 경로를 만들기 전까지 이 상태는 상세에서만 정확하다.
> 6. **카드의 이동장·유모차 문구 4개가 모두 이 절과 어긋난다.** `No carrier needed`·`Hold or stroller indoors`·`Carrier/stroller required`·`Needs confirmation`(및 한국어 대응)이 각각 유모차 누락, 이동장 누락, `또는` 누락, 확인 대상 누락에 해당한다. 위 어휘표의 문구로 맞춘다.

**어휘 규칙**

- **행동 안내를 상태 이름으로 쓰지 않는다.** `Check with the store` / `매장에 문의하세요`는 사용자가 할 일이지 조건의 상태가 아니다. 상태는 `미확인`으로 적고, 문의 안내가 필요하면 상태 옆이나 아래에 별도 문장으로 둔다.
- **허용 크기 하나만 확인됐다고 방문 가능으로 읽히게 쓰지 않는다.** `All sizes welcome`처럼 조건 전체를 통과한 듯한 표현을 크기 값에 쓰지 않는다. 크기는 크기만 말한다. 방문 가능 여부 단언은 §6 Visit eligibility banner에서만 한다.
- 축약형은 **폭이 부족할 때만** 쓴다. 자리가 있으면 정식 문구를 쓴다.
- 축약형은 **같은 화면의 라벨·아이콘·배치를 함께 봤을 때 뜻이 그대로 남는 경우에만** 허용한다. 문맥을 빼고 그 문구만 떼어 놨을 때 다른 뜻으로 읽힌다면 축약하지 않는다. 예를 들어 `필요 여부 미확인`은 이동장 조건 라벨이나 아이콘 옆에 있을 때만 쓸 수 있다.
- 조건 이름(label)은 상세에서 항상 함께 보여준다. 카드에서는 값만 보여도 된다. 단 값만 봤을 때 어떤 조건인지 알 수 있어야 한다 — `미확인` 하나만 단독으로 두지 않는다.
- 한국어와 영어의 **정보 우선순위를 같게** 맞춘다. 한쪽에만 조건 이름을 넣거나 한쪽만 단정적으로 쓰지 않는다.

## 10. Internationalization

- 영어 UI를 기본으로 설계하고 한국어 번역 후 레이아웃이 깨지지 않는지 확인한다.
- 영어 UI: 영문 장소명을 primary, 한국어명을 secondary로 표시한다.
- 한국어 UI: 한국어 장소명을 primary, 영문명을 secondary로 표시한다.
- 번역되지 않은 정보를 번역된 사실처럼 보이게 하지 않는다.
- 날짜, 거리, 시간은 locale에 맞게 표시한다.
- 한국 매장에 전달할 문의 문구는 한국어 원문을 유지한다.
- 버튼 텍스트가 길어지면 너비를 늘리거나 줄바꿈하고 의미를 생략하지 않는다.

### Distance formatting

```text
0 m          근처로 표기            Nearby / 근처
1–999 m      10 m 단위 반올림       620 m / 620m
1–9.9 km     소수 첫째 자리          1.2 km / 1.2km
10 km 이상    정수                  12 km / 12km
```

**언어별 표기**

- 영어: 숫자와 단위 사이에 **공백을 넣는다.** `620 m`, `1.2 km`
- 한국어: **붙여 쓴다.** `620m`, `1.2km`
- 두 언어 모두 m/km를 쓰고 mi로 변환하지 않는다.

**경계 처리**

- 10 m 단위로 반올림한 결과가 `1000 m`가 되면 km 표기로 넘긴다. → `1.0 km` / `1.0km`
- km 표기에서 소수 첫째 자리 반올림 결과가 `10.0` 이상이면 정수로 적는다. → `10 km` / `10km`
- 위치를 확인하지 못했거나 거리 계산이 실패한 경우에는 **거리 영역 자체를 표시하지 않는다.** `—`, `0 m`, `근처` 어느 것으로도 대체하지 않는다.
- `근처` / `Nearby`는 **유효한 계산 결과가 0 m일 때만** 쓴다. 값이 없는 상태를 가리키는 말이 아니다.

**확인일 표기**

| 경과 | 영어 | 한국어 |
|---|---|---|
| 오늘 | `Checked today` | `오늘 확인` |
| 1–7일 | `Checked 3 days ago` | `3일 전 확인` |
| 8일 이상 | `Checked on July 20, 2026` | `2026년 7월 20일 확인` |

- 경계는 **7일까지 상대 표기, 8일부터 절대 표기**다.
- 확인 방법이 있으면 확인일 뒤에 함께 붙인다(§6 Verification Status).
- 90일 재확인 기준은 이 표기 규칙과 **별개**다. 표기 방식이 무엇이든 90일 이상이면 `재확인 필요`를 함께 보여준다(§7).

## 11. Accessibility

목표 등급은 **WCAG 2.1 AA**다. 아래는 표준이 요구하는 것과 이 프로젝트가 스스로 정한 것을 나눠 적었다. 둘을 섞으면 근거를 확인할 수 없다.

### 표준 요구 (WCAG 2.1 AA)

| 항목 | 기준 | 이 문서의 적용 |
|---|---|---|
| 텍스트 대비 (1.4.3) | 4.5:1, 큰 텍스트 3:1 | §4 `Contrast reference`의 측정값으로 판정 |
| 비텍스트 대비 (1.4.11) | 3:1 — **식별에 필요한** UI 요소와 상태에 한정 | 텍스트 입력·검색 필드처럼 경계가 곧 식별 정보인 컨트롤에 적용(§4 `Border color usage`). 순수 장식·구조 선은 대상이 아니다 |
| 색상만으로 전달 금지 (1.4.1) | — | 상태는 색 + 아이콘 + 텍스트로 함께 전달한다 |
| 키보드 조작 (2.1.1) | — | 모든 interaction을 키보드로 도달·조작·이탈할 수 있어야 한다 |
| 포커스 표시 (2.4.7) | — | 모든 interaction에 `focus-visible` 표시를 제공한다 |
| 우회 링크 (2.4.1) | — | skip link로 반복되는 내비게이션을 건너뛴다 |

- 큰 텍스트 예외(3:1)는 **18.66px 이상 굵은 글씨 또는 24px 이상**에만 적용된다. §4 Typography의 Body(14px)·Caption(12px)은 해당하지 않는다.
- 대비 판정은 **반올림 전 값**으로 한다. `4.49`는 통과가 아니다.

### 프로젝트 자체 규칙

표준이 AA에서 요구하지 않지만 이 서비스가 지키기로 한 것들이다.

| 항목 | 규칙 | 비고 |
|---|---|---|
| 조작 영역 | 최소 44×44 CSS px (§6 `크기와 조작 영역`) | WCAG 2.1에서는 **AAA**(2.5.5) 수준이다. AA 요구가 아니다 |
| 지도 대체 수단 | 지도만으로 결과를 제공하지 않고 동일한 장소 목록을 함께 제공한다 | |
| 조건 상태 어휘 | 같은 상태를 화면마다 같은 말로 표기한다 (§9) | |

### 오버레이와 포커스

시트와 다이얼로그를 **모달인지 아닌지**로 나눈다. 둘의 규칙이 다르다.

| 종류 | 예 | focus trap | `role="dialog"` / `aria-modal` | Escape |
|---|---|---|---|---|
| **모달 Dialog** | 필터 Drawer, 반려견 등록·삭제 확인 | **적용한다** | 적용한다 | 닫는다 |
| **비모달 탐색 시트** | 탐색 화면의 Bottom Sheet (§5 Places Mobile) | **적용하지 않는다** | 적용하지 않는다 | 선택 단계에서 이전 단계로 되돌린다 |

- 탐색 시트에 모달 시맨틱을 걸지 않는 이유: 이 시트는 **닫히지 않는 패널**이다. peek 단계가 항상 떠 있고 지도가 그 뒤에서 계속 조작된다. focus trap을 걸면 지도·헤더·`내 위치` 버튼이 키보드와 스크린리더에서 잠긴다. (결정 D-13a)
- 비모달 시트에도 **키보드로 들어가고 나오는 길**은 반드시 있어야 한다. 시트 안팎을 탭으로 오갈 수 있고, 선택 단계에서 벗어나는 수단(닫기 버튼과 Escape)을 둔다.
- 모달이 닫히면 포커스를 **열기 전 위치로 되돌린다.**
- 모션이 끝나기 전에 포커스를 옮기지 않는다. 퇴장 애니메이션 중에 요소가 사라지면 포커스가 문서 처음으로 튄다.

### 상태 변경 안내

**요구사항** — 아래 셋은 확정된 기준이다. 구현 방법은 여러 가지일 수 있지만 결과는 셋을 모두 만족해야 한다.

1. **키보드 탐색을 방해하지 않는다.** 사용자가 목록을 훑는 중에 포커스가 사용자 의도와 무관하게 다른 영역으로 끌려가면 안 된다. 카드를 연달아 비교하는 조작이 끊기지 않아야 한다.
2. **필요한 변경을 인지할 수 있다.** 선택 결과가 바뀌었는데 스크린리더 사용자가 그 사실을 알 방법이 없으면 안 된다.
3. **안내가 불필요하게 중복되지 않는다.** 하나의 사건은 한 번만 알린다. 결과 수 안내와 선택 결과 안내는 서로 다른 사건이며, 한 번의 조작에서 겹쳐 읽히지 않아야 한다.

- 포커스를 옮겨서 알릴지, live region으로 알릴지는 위 셋을 만족하는 선에서 정한다. 같은 내용을 두 방법으로 동시에 내보내지 않는다.
- 안내는 스크린리더에 전달되는 텍스트를 말한다. 소리를 재생하는 기능이 아니다.

> **현재 구현은 미검증.** 선택 시 미리보기 제목으로 포커스를 옮기는 지금 방식이 위 1·3을 만족하는지는 **실제 키보드·스크린리더로 확인하지 않았다.** 위 요구사항 자체는 확정이고, 확인해야 할 것은 현재 구현이 그것을 만족하는지다.

### 구조

- 화면마다 `<main>` 랜드마크가 하나 있고 skip link가 그것을 가리킨다(§5 `Header와 main의 소유 위치`).
- skip link는 페이지의 **첫 번째 초점**이며, 포커스를 받으면 **눈에 보여야 한다.** 숨은 채로 동작만 하면 키보드 사용자가 존재를 알 수 없다.
- 제목은 단계를 건너뛰지 않는다. 화면의 주제를 담은 제목이 하나 있고 그 아래 섹션 제목이 붙는다.
- 의미를 전달하는 아이콘에는 텍스트 라벨을 병기하고, 장식 아이콘은 접근성 트리에서 제외한다(§4 Icons).

## 12. Implementation Guardrails

- Airbnb·Karrot 토큰을 프로젝트에 그대로 복사하지 않는다.
- 예약, 호스트, 거래, 채팅 기능을 디자인에 추가하지 않는다.
- `Pet-friendly` 배지 하나로 동반 조건을 대체하지 않는다.
- 검증일이나 확인 방법이 없으면 임의로 생성하지 않는다.
- 장소 선택 때마다 별도 페이지로 이동시키지 않는다. 탐색 화면에서는 미리보기 패널을 우선 사용한다.
- 지도 컴포넌트를 카드 hover나 선택 때 재마운트하지 않는다.
- 장식용 gradient, glassmorphism, 과도한 emoji를 사용하지 않는다. 그림자는 §4 `그림자 기준`이 배정한 역할에만 쓰고, 그 표에 없는 단계나 임의 값을 만들지 않는다.
- 기존 shadcn/ui와 Tailwind 토큰을 우선 확장하고 동일 역할의 새 컴포넌트를 중복 생성하지 않는다.
- 기존 URL filter, selected place, hover state의 동기화를 깨뜨리지 않는다.
- 모바일을 데스크톱 3분할의 축소판으로 만들지 않는다.
- 다크 모드 스타일을 임의로 추가하지 않는다.
- z-index는 정의된 토큰만 사용한다.
- 테두리 토큰은 §4 `Border color usage`가 정한 역할대로만 쓴다. 구조 선과 컨트롤 경계를 한 토큰으로 처리하지 않는다.
- lucide 외의 아이콘 셋을 추가하지 않는다.

## 13. 문서 대비 구현 후속 작업

**이 절은 기준이 아니라 목록이다.** 위 절들이 정한 기준과 현재 코드가 어긋난 곳을 한자리에 모았다. 각 항목의 판단 근거는 해당 절의 `현재 구현과 어긋난 곳` 상자에 있다.

읽는 규칙 셋:

- **문서에 적혔다는 것이 구현됐다는 뜻이 아니다.** 여기 남아 있는 항목은 아직 화면에 반영되지 않았다.
- **저장할 수 있다는 것도 구현됐다는 뜻이 아니다.** 스키마가 허용하는 것과 화면이 올바르게 보여주는 것을 따로 적었다.
- `확인` 열은 **이번에 코드로 대조한 것**과 이전 감사에서 넘어와 아직 다시 대조하지 않은 것을 구분한다. `미대조`는 문제가 없다는 뜻이 아니라 확인하지 않았다는 뜻이다.

### A. 토큰과 스케일 (§4)

| # | 현재 | 기준 | 확인 |
|---|---|---|---|
| A-1 | `--color-danger`가 `#dc2626` | §4 Colors의 `#d51f1f` | 이번 대조 |
| A-2 | `--color-border-control` 토큰 없음. `border.control` 매핑도 없음 | §4 `Border color usage`·`Token aliases`가 `#828b98` 토큰과 `border.control` 유틸리티를 전제한다 | 이번 대조 |
| A-3 | `text-xs`가 12/16. `tailwind.config.ts`에 `fontSize` 확장 없음 | §4 Typography의 `Caption 12/18` | 이번 대조 |
| A-4 | 관리자 목록의 모바일 장소 카드가 `rounded-xl`(12px) | §4 콘텐츠 카드 16px. 다른 카드는 이미 `rounded-2xl` | 이번 대조 |
| A-5 | `ui/skeleton.tsx`가 `rounded-md` 고정 | §4 Skeleton은 대체할 요소의 radius·크기·배치를 따른다 | 이번 대조 |

### B. 그림자 (§4 `그림자 기준`)

| # | 조치 | 대상 | 개수 |
|---|---|---|---|
| B-1 | **제거** | 콘텐츠 카드 6 + 패널·섹션 컨테이너 9의 `shadow-sm` | 15 |
| B-2 | **교체** | 지도 위 상태 알림 `shadow-sm` → `shadow-md` | 1 |
| B-3 | **교체** | 필터 Drawer `shadow-2xl` → `shadow-lg` | 1 |
| B-4 | **유지 (보류)** | 히어로 장식 `shadow-md`, segmented control thumb `shadow-sm` | 2 |
| B-5 | 교체 (미사용) | `ui/dropdown-menu.tsx` 최상위 표면 `shadow-md` → `shadow-lg` | 1 |

B-4는 값이 미결정일 뿐이며, 이 때문에 B-1~B-3을 미루지 않는다.

### C. 조건 문구와 상태 (§9·§10)

**끝난 것** — 2026-09-08. 코드·테스트·브라우저로 확인했다.

| # | 내용 |
|---|---|
| ~~C-1~~ | 카드의 이동장·유모차 문구 4개(en/ko)를 §9 어휘표에 맞췄다. 상세·미리보기 문구도 함께 맞췄다 |
| ~~C-2~~ | 미리보기가 `NOT_ALLOWED`를 조건 목록에 넣던 분기를 없앴다. 상태 영역이 단독으로 단언한다 |
| ~~C-3~~ | 상세 실내 조건 행의 라벨을 `동반 가능 여부` / `Dog access`로 맞췄다 |
| ~~C-4~~ | 목록 조회·타입·매핑에 `policyDetails`를 실어 카드·미리보기가 구역 기록을 함께 읽는다 |
| ~~C-5~~ | 미참조 키 `places.selectedPlacePanel.conditions.*` — 아래 F-1로 옮긴다(문구가 아니라 정리 대상) |
| ~~C-6~~ | 관리자 표의 영어 라벨 — 아래 C-9로 남긴다 |

**남은 것**

| # | 내용 | 확인 |
|---|---|---|
| C-7 | 날짜·거리 표기가 §10 규칙과 맞는지 | **미대조** |
| C-8 | 카드 `maxDogSize`·상세 `dogSize`의 상한 표기는 맞췄으나, 상세 라벨 `강아지 크기 제한`이 다른 라벨(`동반 가능 여부`·`이동장·유모차`)과 말투가 다르다 | 이번 대조 |
| C-9 | 관리자 표의 영어 라벨이 `Not allowed`라 무엇이 불가한지 없다. 관리자 폼의 `확인 필요`도 조건 UNKNOWN 표기와 어긋난다 | 이번 대조 |

### D. 구조와 접근성 (§5·§6·§11)

| # | 내용 | 확인 |
|---|---|---|
| D-1 | 44×44 조작 영역 전수 적용 여부. 정렬 컨트롤 등 일부는 `h-11`로 확인됨 | 부분 대조 |
| D-2 | Header와 `main` 랜드마크의 소유 위치, 상세 페이지의 정보 표시 순서 | **미대조** |
| D-3 | §11 `상태 변경 안내`의 요구사항 1·3을 현재 구현이 만족하는지 | **실제 동작 미검증** (§11 참조) |

### E. 모션

`plans/` 트랙에서 넘어온 잔여 항목이다. 계획 문서가 따로 있으므로 여기서는 존재만 기록한다.

| # | 내용 |
|---|---|
| E-1 | `DogCard`의 `DropdownMenu`에 진입·퇴장 모션이 없다 |
| E-2 | 카테고리 탭의 스켈레톤 → 카드 전환이 한 프레임에 교체된다 |
| E-3 | 즐겨찾기 토글이 테두리 색만 바뀐다 |

### F. 미사용 코드 정리

**기능 작업이 아니다.** 화면에 영향이 없으므로 위 항목들과 섞어 우선순위를 매기지 않는다.

| # | 내용 |
|---|---|
| F-1 | `places.selectedPlacePanel.conditions.*` — 어느 컴포넌트도 참조하지 않는다 (C-5와 같은 대상) |
| F-2 | `ui/sheet.tsx`·`ui/dropdown-menu.tsx` — 화면에 연결되어 있지 않다 (B-5, E 트랙 감사 #8과 같은 파일) |
| F-3 | `CarrierPolicy` enum — 모델 필드에서 쓰이지 않으며 스키마에만 남아 있다 |

### G. 저장 단계 검증 (후속)

| # | 내용 |
|---|---|
| G-1 | 요약 컬럼과 구역 기록이 **명백히 모순되는 입력**(예: `indoor: ALLOWED` + 전체 반려견 실내 불가)을 저장 단계에서 막을지 정한다. 현재는 저장되고 표시 계층이 `정보 불일치`로 받는다. **정상적인 크기·세부 구역 예외(대형견만 실내 불가, 2층만 불가)와 반드시 구분해야 한다** — 이걸 구분하지 못하는 검증은 정상 데이터를 막는다 |

### 권장 순서

| 순서 | 묶음 | 이유 |
|---|---|---|
| ~~1~~ | ~~C-1 · C-2 · C-3 · C-4~~ | **2026-09-08 완료** |
| 1 | A-1 · A-2 · A-3 | 토큰이 없으면 이후 작업이 문서와 다른 값을 쓰게 된다. 다른 항목의 전제다 |
| 2 | B-1 · B-2 · B-3 | 시각 변경이 넓지만 각 지점은 한 줄이다. 1을 끝낸 뒤 한 번에 훑는다 |
| 3 | A-4 · A-5 | radius 정리. 2와 같은 파일을 여러 개 건드리므로 붙여서 한다 |
| 4 | C-8 · C-9 | 남은 문구 정리. 관리자 화면이 섞여 있어 사용자 화면 작업과 분리한다 |
| 5 | D-1 · D-2 · C-7 | 먼저 전수 대조부터 한다. 대조 결과 없이 고치지 않는다 |
| 6 | G-1 | 저장 검증. 표시 계층이 이미 받아 내고 있으므로 급하지 않고, 정상 예외와의 구분 규칙을 먼저 정해야 한다 |
| 7 | E-1 · E-2 · E-3 | 모션. 위 작업의 전제가 아니다 |
| 8 | F-1 · F-2 · F-3 | 정리. 언제 해도 되지만 B-5·E 감사 #8과 겹치므로 그 결정 뒤에 한다 |

**D-3과 아래 §11의 미검증 항목은 이 순서에 넣지 않는다.** 고칠 대상이 아니라 확인할 대상이며, 확인 결과에 따라 작업이 생길 수도 생기지 않을 수도 있다.

## Included Components

**이 문서가 규칙을 정하는 UI 요소 목록이다.** 컴포넌트 파일 목록이 아니다.

- 여기 있는 이름이 **같은 이름의 파일이 있다는 뜻은 아니다.** 어떤 요소는 별도 컴포넌트로, 어떤 요소는 화면 안에 직접 구현되어 있다.
- 여기 없다고 해서 만들면 안 되는 것도 아니다. 새 UI 요소를 만들면 이 목록과 해당 규칙 절을 함께 늘린다.
- 실제 구현 위치는 코드에서 확인한다. 이 목록으로 구현 여부·사용 여부를 판단하지 않는다.

**탐색·조작**

- 버튼 (primary / secondary / tertiary / destructive) — 구현 예: `ui/button.tsx`
- 아이콘 단독 버튼
- 입력 필드, 검색 필드
- 필터 칩, 카테고리 탭
- 정렬 컨트롤 — 구현 예: `SortDropdown`
- 필터 Drawer — 구현 예: `FilterModal`
- Bottom Sheet (비모달 탐색 시트)
- 모달 Dialog

**장소 정보**

- 장소 카드 — 구현 예: `PlaceCard`
- 조건 요약 — 구현 예: `PlaceConditionSummary`
- 방문 가능 배너 — 구현 예: `EligibilityBanner`
- 조건 행, 조건 배지 — 구현 예: `ConditionBadge`
- 검증 상태 표시(확인일·확인 방법·재확인 필요)
- 방문 전 안내 — 구현 예: `BeforeYouGoCard`
- 미리보기 패널 — 구현 예: `PlacePreviewCard`
- 반려견 매칭 배지 — 구현 예: `DogMatchBadge`
- 즐겨찾기·공유 액션 — 구현 예: `FavoriteButton`, `ShareButton`
- 한국어 문의 박스 — 구현 예: `KoreanInquiryBox` *(MVP 제외 / P1 재도입 예정)*

**지도**

- 지도 패널 — 구현 예: `MapPanel`
- 지도 마커 (기본 / hover / 선택)

**공통 상태**

- 공통 헤더 — 구현 예: `Header`
- 빈 상태, 위치 안내 배너, 서비스 범위 배너
- Skeleton — 구현 예: `ui/skeleton.tsx`
- Snackbar

**관리자**

- 장소 표
- 장소 등록·수정 폼 — 구현 예: `admin/PlaceForm.tsx`